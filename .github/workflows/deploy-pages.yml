name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    name: Validate and build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      BASE_PATH: /${{ github.event.repository.name }}
      SITE_URL: https://${{ github.repository_owner }}.github.io
      PLAYWRIGHT_SERVER: preview
      PUBLIC_GA_MEASUREMENT_ID: "${{ vars.PUBLIC_GA_MEASUREMENT_ID || 'G-6GR5VBYXEQ' }}"
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          package-manager-cache: false
      - name: Enable Corepack
        run: corepack enable
      - name: Verify Yarn version
        run: yarn --version
      - name: Install dependencies
        run: yarn install --immutable
      - name: Run formatting, lint, type and unit/content checks
        run: yarn check
      - name: Build production site
        run: yarn build
      - name: Verify generated routes, links and metadata
        run: yarn verify:build
      - name: Inspect production bundles
        run: yarn inspect:build
      - name: Install Chromium
        run: yarn playwright install --with-deps chromium
      - name: Run end-to-end tests against production output
        run: yarn test:e2e
      - name: Run accessibility tests against production output
        run: yarn test:a11y
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6
      - name: Upload production artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist
  deploy:
    name: Deploy to GitHub Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: build
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
