# Validation records

## Node 24 GitHub Actions and Yarn 4 bootstrap — 9 September 2026

Updated all workflows away from Node-20-based action releases. CI, Pages deployment and scheduled external-link checks now use `actions/checkout@v7` and `actions/setup-node@v7`; Pages configuration uses `actions/configure-pages@v6`. The setup-node cache lookup that invoked global Yarn 1 before Corepack was removed. Each workflow now disables setup-node package-manager caching, enables Corepack, verifies the resolved Yarn version and then performs the immutable install.

| Check                                 | Result                                                                                                                                                                                                                                    |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Workflow parsing and version audit    | PASS; all three workflow YAML files parsed successfully. No `setup-node@v4`, `checkout@v4`, `configure-pages@v5` or premature `cache: yarn` input remains.                                                                                |
| Package-manager bootstrap             | PASS; Node 24 resolved project Yarn 4.17.1, and `yarn install --immutable` completed successfully without using global Yarn Classic.                                                                                                      |
| Format, lint, type and content checks | PASS; `yarn check` completed. Astro checked 164 files with zero errors, warnings or hints; all 56 unit/content tests in 16 files passed.                                                                                                  |
| GitHub Pages production build         | PASS; `/CSE-Pothik/` emitted 185 HTML pages / 210 files without warnings. Generated verification found no broken local URL or SEO/placeholder defect; bundle inspection confirmed compact CSS/JS, responsive assets and zero source maps. |

The workflow definitions and every command they invoke were validated locally. A hosted runner execution requires pushing the commit; no live workflow run or deployment is claimed here.

## Voluntary feedback and production analytics — 9 September 2026

Added one quiet bilingual feedback invitation to the shared footer on every page. It opens the supplied Google Form in a safe new tab and does not interrupt reading with a popup, embed, floating control or primary-navigation item. The form URL is centralized in site configuration. The GitHub Pages workflow now injects GA4 measurement ID `G-6GR5VBYXEQ` only for the production build; local and test builds remain analytics-free unless explicitly configured. Existing bounded events, disabled Google Signals and privacy disclosures remain in force, and search text, form responses, contact details and other free text are never sent to GA4.

| Check                                      | Result                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Format, lint, type and content checks      | PASS; `yarn check` completed. Astro checked 164 files with zero errors, warnings or hints; all 56 unit/content tests in 16 files passed. New integrity checks protect the exact centralized form URL.                                                                                                                                                                                  |
| Production build and generated output      | PASS; the `/CSE-Pothik/` build emitted 185 HTML pages / 210 files without warnings. Generated verification found no broken local URL, unsafe external anchor, SEO/placeholder defect, missing feedback link or missing configured GA4 loader/config call. Bundle inspection confirmed compact one-line CSS/JS, six responsive WebP assets and no source maps.                          |
| Focused browser and accessibility coverage | PASS; 38 Chromium checks passed with 20 intentional duplicate-project/matrix skips. Coverage included English/Bangla feedback labels, URL, safe new-tab behavior, analytics attributes, keyboard/no-JavaScript use, axe WCAG A/AA checks, light/dark themes, console cleanliness and 320, 390, 768, 1280 and 1600px layouts.                                                           |
| Analytics integration                      | PASS within local automated scope; the event test preserved a browser-side `gtag` stub, blocked external collection and verified the bounded interaction events. Production layout tests served a harmless local Google-script stub so network restrictions could not hide genuine page errors. This validates emitted integration and dispatch, not receipt by the live GA4 property. |
| Privacy and documentation                  | PASS; the Privacy page, README, DESIGN, AI-Prompt and SKILL explain the optional Google Forms provider boundary, production-only analytics configuration and prohibited data. Merely seeing the footer link submits nothing.                                                                                                                                                           |

The form's live destination and live GA4-property receipt were not tested from the restricted local environment. No deployment or Google Analytics account mutation was performed.

## GitHub Pages automation and identity documentation — 9 September 2026

Simplified the active README, illustration provenance and rebrand record so they describe only the CSE-Pothik identity. The intentional generated-output regression guard still rejects the retired public name. The existing push-to-`main` Pages workflow was retained, clarified and updated to the current major versions of the official Pages artifact and deployment actions. It derives the project base from the repository name, so renaming the repository to `CSE-Pothik` automatically changes the production base to `/CSE-Pothik/` without another source edit.

| Check                                      | Result                                                                                                                                                                                                                                               |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Workflow structure                         | PASS; the YAML parsed successfully, includes the `main` push trigger, and requires the validated build job before deployment. The workflow uses Corepack, immutable Yarn installation, official Pages permissions/actions and uploads only `dist/`.  |
| Format, lint, type and unit/content checks | PASS; `yarn check` completed. Astro checked 164 files with zero errors, warnings or hints; all 55 unit/content tests in 16 files passed.                                                                                                             |
| `/CSE-Pothik/` production build            | PASS; 185 HTML pages / 210 files built without warnings. Generated verification found no broken local URL, unsafe outbound anchor, SEO/placeholder defect or retired public brand string; bundle inspection found compact CSS/JS and no source maps. |
| Production end-to-end regression           | PASS; 246 checks passed with 106 intentional duplicate-project/matrix skips against the future repository base.                                                                                                                                      |
| Production accessibility regression        | PASS within automated scope; all 142 axe WCAG A/AA checks passed across representative desktop/mobile, English/Bangla and light/dark pages.                                                                                                          |
| Identity documentation                     | PASS; current identity documentation describes CSE-Pothik on its own terms. Historical records of the former product/base-path name and the intentional verifier pattern remain internal and are not emitted as public branding.                     |

No push, repository rename, GitHub Pages settings change or live deployment was performed. The repository owner must select **GitHub Actions** as the Pages publishing source once; subsequent pushes to `main` are automatic.

## CSE-Pothik rebrand — 9 September 2026

Rebranded the product from CSE Compass to **CSE-Pothik** while retaining the established tagline, content model and bilingual mentoring experience. The identity treats the student as the `Pothik` (পথিক)—the person travelling and choosing a path. The original code-native compass-C mark was retained with the new CSE-Pothik wordmark. Metadata, structured data, storage keys, package identity, navigation, credits, templates, tests and documentation use the new name. A matching original social card was generated with the built-in ImageGen tool, visually checked for the exact title/tagline and recorded in `docs/illustrations.md`; the superseded social asset was removed.

| Check                          | Result                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dependency reproducibility     | PASS; `yarn install --immutable` completed with Yarn 4.17.1 after the workspace package identity was updated in the lockfile. No npm command was used.                                                                                                                                                                                                     |
| Format, lint, type and content | PASS; `yarn format` and final `yarn check` completed. Astro checked 164 files with zero errors, warnings or hints; all 55 unit/content tests in 16 files passed.                                                                                                                                                                                           |
| Root production and SEO        | PASS; 185 HTML pages / 210 files built without warnings. Generated-output verification found no broken local URL, unsafe outbound anchor, missing canonical/hreflang/Open Graph/structured metadata, old public brand string or unfinished-copy defect. Bundle inspection confirmed minified CSS/JS, six responsive WebP assets and no source maps.        |
| GitHub Pages base path         | PASS; `SITE_URL=https://amjadhossainrahat.github.io BASE_PATH=/CSE-Pothik` produced the same 185 pages / 210 files and passed generated-output and bundle inspection. The final `dist/` uses `/CSE-Pothik/`. The workflow still infers the real repository name at deployment, so it remains compatible during a repository-rename transition.             |
| Browser regression             | PASS; 246 Playwright checks passed with 106 intentional duplicate-project/matrix skips. Coverage included the visible CSE-Pothik wordmark, social metadata, navigation, journeys, no-JavaScript behavior, GA4 stubs, console/page errors, English/Bangla, light/dark and 320, 390, 768, 1280 and 1600px layouts.                                           |
| Accessibility                  | PASS within automated scope; all 142 axe WCAG A/AA checks passed across representative desktop/mobile, English/Bangla and light/dark pages. The wordmark exposes the centralized full product name while decorative compass geometry stays hidden from assistive technology. Automated results are not formal WCAG certification or a screen-reader study. |
| External learning links        | PASS with two review items; the network-enabled checker verified 84 of 86 curated destinations. Linux Journey and one ACM authorship-policy URL returned HTTP 403 to automation and remain `REVIEW`; no link was confirmed broken.                                                                                                                         |
| Source integrity               | PASS; `git diff --check` found no whitespace defect. Current source, public assets and authoritative docs contain no stale public identity or old asset reference; historical `/CSE-Compass/` results remain only in earlier validation records.                                                                                                           |

No live GitHub repository rename, Pages deployment, physical-device session, Safari/Firefox run, real GA4-property receipt or formal assistive-technology study is claimed.

## AI-era software engineering foundations — 9 September 2026

Reframed the Software Engineering Foundations guide around the review bottleneck created by high-volume AI-assisted code. The page now exposes five linked lenses—Clean Code, Code Smells, SOLID Principles, Design Patterns and Defensive Programming—inside a nine-topic practice sequence that retains Refactoring, design principles, testing and architectural boundaries. Every topic has a distinct AI-era reason, learning timing, enough-for-now depth, practice and misuse warning in English and conversational Bangla. The homepage AI narrative, shared professional roadmap stage and bilingual static search surface the same concepts without adding a route or homepage section.

| Check                          | Result                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Format, lint, type and content | PASS; final `yarn check` covered 164 files with zero Astro errors, warnings or hints and all 55 unit/content tests passed. Resource integrity checks protect the three exact supplied URLs and bilingual search coverage.                                                                                                                                                           |
| Complete browser regression    | PASS; 246 tests passed with 106 intentional duplicate-project/matrix skips. The new eight-check English/Bangla desktop/mobile suite protects topic order, all nine AI-era explanations, internal lens anchors, viewport containment, safe new-tab references and no-JavaScript content. Existing navigation, themes, GA4 stubs and console/page-error coverage also remained green. |
| Accessibility                  | PASS within automated scope; all 142 axe WCAG A/AA checks passed. The foundation guide is now a critical route in English/Bangla and light/dark across desktop/mobile projects. Automated results are not formal WCAG or screen-reader certification.                                                                                                                               |
| Root production and SEO        | PASS; 185 HTML pages / 210 files built without warnings. `yarn verify:build` found no broken local URL, anchor, SEO metadata or unfinished-copy defect; bundle inspection confirmed minified CSS/JS, six responsive WebP assets and no source maps.                                                                                                                                 |
| GitHub Pages                   | PASS; `/CSE-Compass/` built the same 185 pages / 210 files and passed generated verification and bundle inspection. Fourteen affected behavior/accessibility checks passed against the project base. Final `dist/` uses the project base.                                                                                                                                           |
| Learning resources             | PASS with one review item; the network-enabled audit verified 85 of 86 destinations with HTTP 200, including all three requested Refactoring.Guru pages. Linux Journey returned its known automated HTTP 403 and remains `REVIEW`; no link was confirmed broken.                                                                                                                    |
| Documentation                  | PASS; README, DESIGN, AI-Prompt, SKILL, UX review and this record describe the same learning order, AI-era purpose, defensive-programming boundary and scoped-source policy. `git diff --check` is part of final review.                                                                                                                                                            |

The first focused E2E run used an ambiguous link-name selector and selected the internal Code Smells anchor instead of the identically named external guide. The implementation was correct; the test was narrowed to the external URL and all eight focused checks, then the complete suite, passed. A duplicate critical-route entry introduced while extending accessibility coverage was removed before the final 142-test run. No live deployment, physical-device/Safari/Firefox session, real GA4-property receipt or formal assistive-technology study is claimed.

## Unified context-bar search and navigation — 9 September 2026

Placed Search, language and theme inside the existing sticky breadcrumb context bar, removed the interim separate utility strip and deleted both localized Search pages. Desktop exposes an inline field and button; mobile/tablet reveals the same controls from a labelled icon without leaving the current route. Results come from a lazy-loaded bilingual static JSON index, stay capped at ten and never persist, transmit or track query text. Keyboard Escape closes the panel and restores focus; the no-JavaScript experience omits the inactive form while preserving all essential navigation.

| Check                          | Result                                                                                                                                                                                                                                                                                                                 |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Format, lint, type and content | PASS; `yarn format` and final `yarn check` completed. Astro checked 163 files with zero errors, warnings or hints; all 55 unit/content tests passed.                                                                                                                                                                   |
| Root production build          | PASS; 185 HTML pages / 210 files built without warnings after removing the two Search routes. Generated verification found no broken local URL, anchor, SEO metadata or placeholder defect; bundle inspection confirmed minified CSS/JS, six responsive WebP assets and no source maps.                                |
| Complete browser regression    | PASS; 238 tests passed with 106 intentional duplicate-project/matrix skips. Coverage included the unified context bar, English/Bangla inline results, route preservation, keyboard/focus behavior, mobile panel containment, no-JavaScript flows, GA4 event stubs, console errors, both themes and 320–1600px layouts. |
| Accessibility                  | PASS within automated scope; all 136 axe tests passed across representative desktop/mobile, English/Bangla and light/dark routes, including the unified context bar, Resources and About. This is not formal WCAG certification or a screen-reader study.                                                              |
| GitHub Pages                   | PASS; `BASE_PATH=/CSE-Compass` produced 185 pages / 210 files and passed URL/SEO verification and bundle inspection. A focused project-base navigation/search regression passed 23 tests with one intentional duplicate-project skip. Final `dist/` uses the project base.                                             |
| External links                 | PASS with one review item; the network-enabled checker verified 83/84 destinations with HTTP 200, including _The Accidental CTO_. Linux Journey’s automated HTTP 403 requires manual review; no link was confirmed broken.                                                                                             |
| Responsive interaction         | PASS within automated Chromium scope; desktop keeps field/button and breadcrumbs on one bar, mobile/tablet uses a viewport-bounded disclosure, and the final full suite covered 320, 390, 768, 1280 and 1600px. Search closes with Escape and restores trigger focus.                                                  |
| SEO/share preview              | PASS; the root preview title/description, canonical, English/Bangla/x-default alternates, sitemap/robots integration, Open Graph/X image and localized alt text were verified. The deleted Search routes and JSON search endpoint are absent from the sitemap; all essential content remains statically rendered.      |

## Persistent breadcrumb navigation — 8 September 2026

Moved every non-homepage breadcrumb into one shared sticky locator strip. The first implementation placed it at the viewport top beside the fixed desktop sidebar and below the compact mobile header; the later utility-bar revision recorded above now keeps it immediately below the desktop bar as well. When JavaScript is disabled and the mobile header becomes non-sticky, the breadcrumb moves to `top: 0`. Narrow layouts keep one row and truncate only the current-page visual label. The complete text remains in the DOM, the full page title follows in content and exactly one breadcrumb item now receives `aria-current="page"`; an existing intermediate-current semantics defect was found during the first regression run and fixed before final validation.

| Check                                      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type and content checks  | PASS; `yarn format` and `yarn check` completed. Astro checked 164 files with zero errors, warnings or hints; all 55 unit/content tests in 16 files passed.                                                                                                                                                                                                                                                                                    |
| Root production build                      | PASS; 187 HTML pages / 211 files built without warnings. Generated verification found no broken local URL, anchor, SEO or unfinished-copy defect; output inspection confirmed minified CSS/JS bundles, six responsive WebP files and no source maps.                                                                                                                                                                                          |
| Sticky navigation and whole-route geometry | PASS after one test-driven semantics fix; 21 tests passed with three intentional duplicate-project skips. The suite scrolled long English and Bangla guidance at 320px, 390px, 768px and 1280px, verified the 72px mobile-header offset and 0px desktop offset, exactly one current crumb, no horizontal overflow, no-JavaScript `top: 0`, existing alignment, sidebar/drawer behavior and every public English/Bangla route’s desktop frame. |
| Accessibility                              | PASS within automated scope; all 108 critical-route axe checks passed across desktop/mobile, English/Bangla and light/dark routes. This is not formal WCAG certification or a screen-reader study.                                                                                                                                                                                                                                            |
| GitHub Pages production                    | PASS; `BASE_PATH=/CSE-Compass` built the same 187 HTML pages / 211 files, passed generated verification and output inspection, and all 18 focused desktop/mobile navigation tests passed. The final `dist/` is this project-base output.                                                                                                                                                                                                      |
| Documentation and repository review        | PASS; README, DESIGN, AI-Prompt, SKILL, UX review and this record describe the shared sticky behavior, truncation, accessibility semantics, anchor clearance and no-JavaScript offset. `git diff --check` is part of the final review.                                                                                                                                                                                                        |

## Problem-solving-aware industry entry and naïve-student UX review — 8 September 2026

Added three generic first-role entry strategies for students with strong competitive problem solving, an inconsistent foundation or little CP experience. Each route states the honest signal, immediate strategy, next evidence and apply-when checkpoint; none names a local employer or presents an adjacent role as an automatic shortcut. Exact-anchor bridges now connect the chooser from the homepage, intent navigation, both situation flows, search, local-industry preparation and Competitive Programming. A fresh-session browser walkthrough followed homepage → new-student plan → first-role/local-industry/CP guidance and informed the placement; this was a heuristic naïve-user review, not a representative student study.

| Check                                      | Result                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type and content checks  | PASS; final `yarn check` completed with 164 Astro/source files reporting zero errors, warnings or hints and all 55 unit/content tests in 16 files passing. New tests protect the three IDs, complete English/Bangla route fields, generic local-industry boundary and exact discovery anchors.                                                                                                                                |
| Root production build and generated output | PASS; `/` built 187 HTML pages / 211 files without warnings. `yarn verify:build` found no broken local URL, anchor, SEO or unfinished-copy defect; `yarn inspect:build` confirmed minified one-line CSS/JS bundles, six responsive WebP assets and zero source maps.                                                                                                                                                          |
| Root browser regression                    | PASS; the complete production-preview E2E run finished with 232 passes, 106 intentional duplicate-project/matrix skips and zero failures. It covered all public-route desktop bounds, navigation, search, “I’m Lost,” first-role/local-industry/CP journeys, no-JavaScript behavior, English/Bangla, both themes, GA4 stubs and desktop/mobile profiles.                                                                      |
| Accessibility                              | PASS within automated scope; the complete root axe suite passed 140/140 tests. Project-base critical routes and homepage entry passed 116/116, including the English/Bangla first-role and Competitive Programming pages in light/dark and desktop/mobile coverage. This is not formal WCAG certification or a screen-reader study.                                                                                           |
| GitHub Pages production                    | PASS; `BASE_PATH=/CSE-Compass` built the same 187 HTML pages / 211 files and passed generated verification and output inspection. Affected production-preview navigation and guidance journeys passed 76 tests with 22 intentional skips; the final `dist/` is this project-base output.                                                                                                                                      |
| Manual browser and console review          | PASS within reviewed scope; the entry-route anchor, all three route cards, local-industry bridge and CP bridge were inspected in the in-app Chromium browser. English and conversational Bangla, dark and light themes, the 771px tablet layout, internal anchor behavior and zero horizontal overflow were verified; the browser reported no warning or error logs. Automated matrices cover 320, 390, 768, 1280 and 1600px. |
| SEO and structured output                  | PASS; generated verification covered canonical URLs, sitemap, robots, metadata and hreflang. Direct inspection of the first-role output confirmed its Pages-aware canonical URL, `en`, `bn` and `x-default` alternates, Open Graph title/description/URL/image, two JSON-LD blocks and both localized sitemap entries.                                                                                                        |
| External resources                         | PARTIAL PASS; the network-enabled checker returned HTTP 200 for 82 of 83 curated URLs. Shafaet’s algorithm index returned HTTP 500 to the automated checker, while an independent live web open still resolved and exposed the intended algorithm catalog. The authoritative URL was retained and this transient/server-specific response is not concealed as a pass.                                                         |
| Repository review                          | PASS; README, DESIGN, AI-Prompt, SKILL, UX review and this record describe the same three-route model and placement rules. Final `git diff --check` and unfinished-copy checks were clean; unrelated work was preserved.                                                                                                                                                                                                      |

## Conversational Bangla editorial pass — 8 September 2026

The Bangla edition was rewritten as original mentoring copy rather than a sentence-by-sentence mechanical translation. Shared navigation and labels, homepage orientation, AI guidance, career/roadmap/experiment templates, search, recovery and fresher routes, first-role guidance, goal introductions, career-family summaries and the complete final-year-project standard now use a consistent respectful `তুমি` voice and natural Bangla sentence order. Established technical vocabulary remains English where it improves recognition or precision. Visual review found and corrected remaining English breadcrumb and eyebrow labels on the first-role and final-year-project pages. A new content test guards representative mentoring surfaces against formal-pronoun drift and known English-only Bangla labels.

| Check                                      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Formatting, lint, type and content checks  | PASS; `yarn format` and final `yarn check` completed. Astro checked 164 files with zero errors, warnings or hints; all 54 unit/content tests in 16 files passed.                                                                                                                                                                                                                                                                                                               |
| Root production build and generated output | PASS; `/` built 187 HTML pages / 211 files without build warnings. Correctly configured generated verification found no broken local URL, anchor, SEO or unfinished-copy defect. Output inspection confirmed minified one-line CSS/JS bundles, six responsive WebP assets and zero source maps.                                                                                                                                                                                |
| Root browser regression                    | PASS; the corrected full production-preview E2E run completed 230 tests with 106 intentional duplicate-project/matrix skips and zero failures. Coverage includes English/Bangla routing, main journeys, themes, keyboard/no-JavaScript use, GA4 stubs, console/page errors, every-public-route desktop bounds and 320/390/768/1280/1600px matrices. Three initial failures were stale exact-copy expectations; those assertions were updated and the complete suite was rerun. |
| Accessibility                              | PASS within automated scope; the complete root axe suite passed 140/140 tests across desktop/mobile, English/Bangla and light/dark. The project-base critical-route axe suite passed 108/108. Automated results do not establish full WCAG conformance.                                                                                                                                                                                                                        |
| Visual and responsive review               | PASS within reviewed scope; actual Bangla screenshots covered the homepage at 1440px, first-role guide at 390px dark and final-year guide at 1280px light. Typography, wrapping, content bounds and theme contrast were readable. The first screenshots exposed English breadcrumb/eyebrow remnants; these were fixed before the final build and browser reruns.                                                                                                               |
| GitHub Pages production                    | PASS; `/CSE-Compass/` built the same 187 HTML pages / 211 files. With the required `BASE_PATH=/CSE-Compass`, generated verification found no broken route, asset, anchor, SEO or placeholder defect. Affected project-base production-preview journeys passed 84 tests with 22 intentional duplicate-project skips; critical axe coverage passed 108/108. The final `dist/` is this project-base output.                                                                       |
| Documentation and repository checks        | PASS; README, DESIGN, AI-Prompt, SKILL, UX review and this record describe the conversational voice and regression rule. `git diff --check` was clean. The unfinished-copy scan found only instructions/tests that mention the strings they intentionally detect.                                                                                                                                                                                                              |

One verifier invocation was initially run without forwarding `BASE_PATH` after the project build and therefore interpreted every valid `/CSE-Compass/...` URL as broken. It was rerun against the unchanged generated output with the correct environment and passed; the misconfigured invocation is not counted as a site failure. No external link catalog content changed in this editorial revision, so the network audit was not rerun. No native-speaker usability study, screen-reader session, physical-device test, Safari/Firefox run, live deployment or live GA4-property receipt is claimed.

## Compact AI panels and sitewide callout audit — 8 September 2026

The AI hub's “A path for builders” and AI Engineering roadmap's “First project” sections now use restrained headings and balanced two-column action panels rather than page-hero typography in cramped three-column rows. Explanation and CTA stay together on desktop; both panels stack in semantic order on mobile. A shared callout-heading guard addresses the same hierarchy risk throughout the site. The curated media label is now **Bangladesh tech leaders & practitioners**, which recognizes the speakers without assigning blanket historical “pioneer” status.

| Check                                         | Result                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type and content checks     | PASS; `yarn format` and `yarn check` completed. Astro checked 163 files with zero errors, warnings or hints; all 52 unit/content tests in 15 files passed.                                                                                                                                                                                                                                                                              |
| Root production browser regression            | PASS; `BASE_PATH=/` production preview completed 230 tests with 106 intentional duplicate-project/matrix skips. Coverage includes every public English/Bangla route, panel desktop/mobile geometry, heading scale, themes, navigation, no-JavaScript behavior, analytics stubs and console/page errors.                                                                                                                                 |
| Accessibility                                 | PASS within automated scope; the complete desktop/mobile axe run passed all 140 tests across English/Bangla and light/dark variants, including AI, local voices, homepage, final-year and first-role journeys.                                                                                                                                                                                                                          |
| Visual and responsive review                  | PASS within tested scope; English desktop and Bangla mobile screenshots of both redesigned panels were inspected. The desktop panels use both columns without awkward title wrapping, while mobile preserves readable stacking. The updated media label was also inspected on desktop.                                                                                                                                                  |
| Sitewide similar-issue audit                  | PASS; the generated every-public-route desktop test checks frame containment, horizontal overflow, breadcrumbs, top-section utilization and now rejects page-scale headings inside any shared callout.                                                                                                                                                                                                                                  |
| External resources                            | PASS with one review item; the network-enabled audit checked all 83 catalogued destinations. Eighty-two returned HTTP 200. Linux Journey returned its known automation-blocking HTTP 403 and remains `REVIEW`; no link was confirmed broken. A transient first-run Tesla Careers timeout returned HTTP 200 on the required retry.                                                                                                       |
| GitHub Pages production, SEO and asset checks | PASS; `/CSE-Compass/` built 187 HTML pages / 211 files without warnings. `yarn verify:build` found no broken local URL, anchor, SEO or unfinished-copy defect; `yarn inspect:build` confirmed compact one-line CSS/JS, six responsive WebP assets and zero source maps. The affected project-base production-preview suite passed 127 tests with three intentional mobile-project skips. The final `dist/` is this project-base output. |

Genuine limitations: automated accessibility checks do not establish complete WCAG conformance, and Chromium emulation is not a substitute for physical devices, Safari, Firefox or formal assistive-technology testing. The wording choice is editorially cautious; no historical ranking of the featured speakers is claimed.

## Concept-first AI systems path and lost-flow watchlist — 7 September 2026

The AI Engineering roadmap now explains how an AI product grows from a defined need into a dependable system: deterministic baseline → testable prompt contract → grounded retrieval/RAG → visible workflow → bounded agent loop → production trust. n8n and OpenClaw are placed as optional implementations after their underlying engineering concepts; multi-agent work waits for a measured coordination need, and AGI is explicitly a research horizon rather than a beginner build step. The AI hub, detailed-roadmap hero and bilingual static search link directly to the anchored path. “I’m Lost” adds a distinct grounded-perspective choice that reaches the ordered nine-video Bangladesh technology watchlist with or without JavaScript.

The complete watchlist heading no longer inherits its narrow editorial measure on desktop, so the English sentence renders on one line when the content frame has room and continues to wrap naturally on smaller screens. Visual review also found and corrected an imbalanced desktop source shelf in the new AI path: its four official references now use a full-width two-column shelf instead of creating a tall right column and a large empty left area.

| Check                                         | Result                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type and content checks     | PASS; final formatting/check commands covered 163 Astro/source files with zero errors, warnings or hints. All 52 unit/content tests in 15 files passed. New contracts protect the six ordered bilingual steps, requested terminology, official source IDs, detailed-roadmap coverage, search anchor and EN/BN “I’m Lost” destinations.                                                                                                 |
| Root production build and SEO verification    | PASS; `/` built 187 HTML pages / 211 files with no warning. Generated verification found no broken local URL/anchor, placeholder or SEO defect. Output inspection confirmed minified bundled CSS/JS, six responsive WebP assets and zero source maps.                                                                                                                                                                                  |
| Root browser and accessibility regression     | PASS; the complete E2E run produced 230 passes and 106 intentional duplicate-project/matrix skips. The complete accessibility run produced 140 passes. Coverage includes desktop/mobile projects, English/Bangla, light/dark, no-JavaScript paths, static search, “I’m Lost”, local voices, navigation, analytics stubs, console/page-error checks and the existing five-width matrices.                                               |
| GitHub Pages production verification          | PASS; `/CSE-Compass/` built the same 187 HTML pages / 211 files and passed generated route, asset, anchor and SEO checks. The affected production-preview suite passed 136 tests across desktop/mobile, critical light/dark axe routes and the changed journeys. After the visual shelf correction, all six focused AI-path production checks passed again. The final `dist/` is this project-base output.                             |
| Responsive, language, theme and visual review | PASS within tested scope; changed content is shared bilingual data, the path renders 3/2/1 columns, and browser checks found no horizontal overflow. English desktop and Bangla mobile screenshots of both the AI path and watchlist were inspected. The desktop watchlist heading is asserted as one rendered line; mobile wrapping remains natural. The final AI shelf was re-inspected after replacing the unbalanced split layout. |
| Accessibility                                 | PASS within automated scope; the new EN/BN AI Engineering roadmap routes were added to critical axe coverage, including dark theme, and passed in desktop/mobile projects. Ordered lists, headings, internal anchors, keyboard/no-JavaScript access, secure external-link descriptions and the progressively enhanced “I’m Lost” fallback remain available.                                                                            |
| SEO and structured data                       | PASS at root and project bases; the existing verifier checked unique titles/descriptions/canonicals, EN/BN/x-default hreflang, Open Graph, valid JSON-LD, sitemap membership, robots sitemap URL, route/asset/anchor integrity and placeholder detection. The change reuses the canonical AI Engineering route instead of creating a duplicate/thin page, while static search links to a crawlable same-page anchor.                   |
| GA4                                           | PASS through the existing local analytics-stub regression. New official resource cards reuse the bounded `learning_resource_clicked` contract with catalog IDs/providers; new internal discovery links do not send search text or student choices. No live GA4-property receipt was claimed.                                                                                                                                           |
| External resources                            | PASS with one review item; the network-enabled audit checked all 83 catalogued destinations. Eighty-two returned HTTP 200, including OpenAI's agent guide, Google Cloud's RAG overview, the n8n learning path, OpenClaw agent documentation and all nine supplied videos. Linux Journey returned its known automation-blocking HTTP 403 and remains `REVIEW`; no link was confirmed broken.                                            |
| Documentation and repository integrity        | PASS; README, DESIGN, AI-Prompt, SKILL, UX review and this record describe the same information architecture, terminology boundaries, source placement and test contract. Final formatting, generated verification, whitespace inspection and unfinished-copy scanning are recorded only after execution.                                                                                                                              |

Genuine limitations: the ordering is a reasoned curriculum, not evidence that one path fits every student; representative usability research is still needed. OpenClaw, n8n and wider AI terminology can change after this review date. Browser coverage uses Chromium emulation rather than physical devices, Firefox, Safari or formal assistive-technology certification. No live GitHub Pages deployment or live GA4-property receipt was performed. Linux Journey's server still blocks automated checking with HTTP 403.

## Curated Bangladesh technology voices — 7 September 2026

The nine supplied YouTube videos and podcast episodes now form a deliberate three-stage sequence: **Ground yourself → Strengthen how you think → Read the working world**. Two starting perspectives replace the homepage's anonymous sample quotations; the complete list appears near the top of Resources. Each item names the speaker and publisher available from the public metadata, preserves the published title where useful, and adds bilingual editorial guidance about what to listen for. A four-question credibility filter helps students distinguish context and trade-offs from hype or fear. The list is explicitly a set of perspectives, not endorsements, statistics, job guarantees or a popularity ranking.

The implementation is typed shared data rendered by one static Astro component. Native disclosures, visible stage-level search anchors and ordinary outbound links preserve keyboard and no-JavaScript access. Links use the site's secure, accessibly announced new-tab contract and bounded `learning_resource_clicked` analytics attributes. No YouTube iframe, autoplay, remote thumbnail, client framework, hydration or new dependency was added.

| Check                                           | Result                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type and content checks       | PASS; final `yarn format` and `yarn check` covered 159 Astro/source files with zero errors, warnings or hints, plus 48 unit/content tests in 14 files. New integrity assertions protect all nine exact video IDs, order, stage membership, localization, review dates, featured items and searchable destinations.                                                                                                            |
| Root production browser and accessibility suite | PASS; the complete E2E and accessibility run passed 356 tests with 106 intentional duplicate-project/matrix skips. Coverage includes the new homepage preview and nine-item list, exact order, native disclosure, keyboard and no-JavaScript access, search, navigation, GA4 stubs, console/page errors, responsive layouts, both languages and both themes.                                                                  |
| Root production build and SEO verification      | PASS; `/` produced 187 HTML pages / 210 files without warnings. `yarn verify:build` found no broken local URL, anchor, SEO or unfinished-copy defect. `yarn inspect:build` reported one-line minified CSS/JS, six responsive WebP assets and zero source maps.                                                                                                                                                                |
| GitHub Pages build and regression               | PASS; `/CSE-Compass/` produced the same 187 HTML pages / 210 files without warnings and passed generated verification. A 148-test affected production suite passed with no skip or failure, covering the watchlist, homepage, static search, analytics, complete critical-route axe matrix, desktop/mobile, English/Bangla, light/dark and no-JavaScript behavior. The final `dist/` is this project-base output.             |
| Accessibility                                   | PASS within automated scope; dedicated watchlist axe scans passed with all stages expanded in English/Bangla, light/dark and desktop/mobile projects. The complete critical-route matrix also passed. Semantic ordered lists, native `details`/`summary`, meaningful headings, localized labels and announced external-link behavior remain intact.                                                                           |
| Responsive, theme, language and visual review   | PASS; automated checks found no horizontal overflow or console/page error. Browser screenshots were inspected in English light and Bangla light/dark at a tablet-width viewport, including expanded two-column cards; phone automation confirms a single-column list and desktop automation covers the wider grid. Typography, card spacing, stage counts and contrast remained readable.                                     |
| SEO and structured data                         | PASS at root and project bases; generated verification covers unique titles/descriptions/canonicals, EN/BN/x-default hreflang, Open Graph, valid JSON-LD, sitemap membership, robots sitemap URL, route/assets/anchor integrity and placeholder detection. The Resources `CollectionPage`/`ItemList` now contains the nine stable watchlist items, and the static bilingual search indexes them without collecting the query. |
| GA4                                             | PASS through the existing local GA4 stub at both bases. Every video link uses a bounded catalog ID and provider with `learning_resource_clicked`; no title, search text or personal data is sent. Live-property receipt was not claimed.                                                                                                                                                                                      |
| External resources                              | PASS with one review item; the expanded live audit checked all nine YouTube URLs and every cataloged guidance/learning destination. Seventy-eight returned HTTP 200, including all supplied videos. Linux Journey returned an automation-blocking HTTP 403 and remains `REVIEW`; no URL was confirmed dead or broken. Public YouTube metadata had also been checked while authoring titles and publishers.                    |
| Documentation and repository integrity          | PASS; README, DESIGN, AI-Prompt, SKILL, the UX review, link checker and this record describe the same data ownership, ordering, editorial boundary, delivery and test contract. `git diff --check` and an unfinished-copy scan are part of the final repository review.                                                                                                                                                       |

Genuine limitations: a curated order and automated/browser checks cannot prove which speakers a fresher should trust or whether the sequence is motivating; that requires moderated research with several students. YouTube content and external destinations can change after review. Browser coverage uses Chromium emulation rather than physical devices, Firefox, Safari or formal assistive-technology certification. No live GitHub Pages deployment or live GA4-property receipt was performed.

## AI-era judgment, motivation and shorter homepage scan — 7 September 2026

The homepage now explains near the beginning why fundamentals become more—not less—valuable when AI can produce plausible code quickly. A visible **Frame → Verify → Own** decision tree asks whether a student can define the problem and constraints, test the result against requirements and fundamentals, and make the trade-off or recovery decision when a tool or lead is unavailable. An expandable workplace story contrasts a fresher prompting with little context, a context-rich assistant reproducing a lead's advanced patterns, and an engineer who can challenge both outputs. The copy preserves an important boundary: coding fluency, algorithms and data structures remain useful, while typing speed alone is not professional judgment; SOLID and design patterns are decision vocabulary, not a ceremonial checklist. The claims are grounded by linked DORA 2025 and NIST NCCoE guidance.

The homepage still exposes every career family and next-step category, but moves the long career-link lists and six-step learning loop into native disclosures. This reduces compulsory scrolling without hiding routes from keyboard, no-JavaScript or assistive-technology users. The existing inclusive study-team and AI-robot illustrations remain purposeful anchors; the decision tree adds a diagrammatic visual and the robot gains a restrained CSS signal pulse that is disabled under reduced-motion preferences. No autoplay, carousel, decorative stock imagery, client framework or hydration was added. The About/footer collaboration credit now names **Codex · GPT-5.6 Sol (High)** in both languages, while retaining Amjad Hossain's role as creator and orchestrator.

| Check                                           | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type and content checks       | PASS; `yarn format` and `yarn check` completed with 154 Astro/source files, zero errors, warnings or hints, and all 45 unit/content tests in 13 files. New assertions protect the bilingual AI message, three decision nodes, three scenario outcomes, evidence relationships and exact model/effort credit.                                                                                                                                                                                                                                          |
| Root production browser and accessibility suite | PASS after fixing two regressions found during the first broad run: the 390px two-column orientation cards were too narrow, and GA4 coverage attempted to click links inside a closed disclosure using stale selectors. The corrected complete `/` suite passed 338 tests with 106 intentional duplicate-project/matrix skips. It covers all E2E and configured accessibility suites, public-route frame containment, navigation, keyboard/no-JavaScript behavior, GA4 stubs, console/page errors, both languages/themes and desktop/mobile projects. |
| GitHub Pages production build                   | PASS; `/CSE-Compass/` built 187 HTML pages / 209 files without warnings. `yarn verify:build` found no broken local URL, anchor, SEO or unfinished-copy defect. `yarn inspect:build` reported compact one-line CSS/JS, six responsive WebP assets and zero source maps. The final `dist/` is this project-base output.                                                                                                                                                                                                                                 |
| GitHub Pages browser regression                 | PASS; the representative base-path suite passed 170 tests with 22 intentional duplicate matrix skips. It covers homepage entry/layout, complete critical-route WCAG scans, core journeys, GA4 integration, English/Bangla, light/dark, 320/390/768/1280/1600px, no-JavaScript, reduced motion and console/page-error capture.                                                                                                                                                                                                                         |
| Responsive, theme, language and visual review   | PASS; the full automated matrix found no horizontal overflow or console error. Fresh screenshots/measurements covered English desktop and Bangla 390px mobile. The desktop decision flow reads as three columns; the phone flow stacks, and phone orientation/family cards remain single-column until 480px so captions retain useful width. Initial collapsed document height measured 5,993px at 1280×900 and 9,997px at 390×844; all detail remains available on demand.                                                                           |
| Accessibility                                   | PASS within automated scope; the expanded AI story and learning loop, homepage entry choices and critical routes produced no configured WCAG A/AA axe findings in English/Bangla, light/dark and desktop/mobile projects. Native `details` preserves keyboard/no-JavaScript access, meaningful headings remain ordered, external evidence links announce new-tab behavior, and the new pulse respects `prefers-reduced-motion`.                                                                                                                       |
| SEO and structured data                         | PASS at root and project bases; generated verification covers unique titles/descriptions/canonicals, EN/BN/x-default hreflang, Open Graph, valid JSON-LD, sitemap membership, robots sitemap URL, route/assets/anchor integrity and placeholder detection. The new evidence links are ordinary crawlable anchors; no duplicate/thin route was introduced.                                                                                                                                                                                             |
| GA4                                             | PASS at both bases using the local GA4 stub while Google requests were blocked. Theme/language, entry, career, roadmap, experiment and AI-guidance events remained bounded; the disclosure change adds no personal data or query-text collection. Live-property delivery was not claimed.                                                                                                                                                                                                                                                             |
| External resources                              | REVIEW, not a clean pass. Two network-enabled audits covered 70 URLs. DORA, NIST and 66 other destinations returned HTTP 200 on the first run; Linux Journey returned an automation-blocking 403. Shafaet's Planet timed out twice. A GitHub resource passed once and had a transient fetch failure on the retry. No URL was confirmed as a dead/404 destination, but `yarn check:external-links` correctly exited non-zero.                                                                                                                          |
| Documentation and repository integrity          | PASS; README, DESIGN, AI-Prompt, SKILL, the UX review and this record describe the AI-era message, progressive disclosure, motion boundary, evidence and model credit. `git diff --check` reported no whitespace defect, and a source scan found no unfinished TODO/FIXME/TBD/Lorem/Coming-soon content.                                                                                                                                                                                                                                              |

Genuine limitations: “best UX” and “not boring” cannot be established without representative moderated research; the visual approach is deliberately informative rather than image-heavy. Browser validation uses Chromium emulation, not physical devices, Safari, Firefox or formal assistive-technology certification. External sites can be transient or block automation as recorded above. No live GitHub Pages deployment or live GA4-property receipt was performed.

## Outbound-link policy and local-industry source — 3 September 2026

The requested single-employer career source has been removed from the source catalog and every guide that referenced it. The local-industry journey now uses the broader BASIS IT industry catalog to help students discover categories of Bangladeshi technology employers before checking vacancies on each employer's official site. All published outbound links—including learning resources, guidance sources, inspiration and creator credits, and the repository—now open in a new tab with `target="_blank"`, `rel="noopener noreferrer"`, and a shared localized accessible description. Internal routes, language equivalents, breadcrumbs and same-page anchors intentionally remain in the current tab so browser history and site navigation stay predictable.

| Check                                     | Result                                                                                                                                                                                                                                                                                    |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type and content checks | PASS; `yarn format` and `yarn check` completed with 154 Astro/source files, zero errors, warnings or hints, and all 44 unit/content tests in 13 files. Integrity coverage requires the replacement BASIS source.                                                                          |
| Root production build                     | PASS; `/` produced 187 HTML pages / 209 files without build warnings. `yarn verify:build` found no broken local URL, anchor, SEO or placeholder defect and enforced the target, safe-rel and accessible-description contract on every generated absolute HTTP(S) anchor.                  |
| Root browser and accessibility regression | PASS; the affected production suite passed 222 tests with 62 intentional duplicate-project/matrix skips. Coverage includes outbound-link behavior, source analytics, keyboard/no-JavaScript flows, English/Bangla, light/dark, five responsive widths and configured WCAG A/AA axe scans. |
| GitHub Pages build and regression         | PASS; `/CSE-Compass/` produced the same 187 HTML pages / 209 files, passed generated build verification and passed the same 222-test browser/accessibility suite with 62 intentional skips. The final `dist/` is this verified project-base output.                                       |
| External resources                        | PASS with one review item; 67 of 68 published external URLs returned HTTP 200, including the replacement BASIS catalog. Linux Journey returned HTTP 403 to automation and remains `REVIEW`; no URL was confirmed broken.                                                                  |
| Documentation and repository integrity    | PASS; README, DESIGN, AI-Prompt and SKILL now agree that outbound links use the secure/accessibly announced new-tab contract while internal navigation stays same-tab. A repository source scan found no remaining reference to the removed source outside generated/dependency folders.  |

Genuine limitations: external destinations can change after validation, and Linux Journey blocks the automated checker with HTTP 403. Browser checks use Chromium emulation rather than physical devices, Firefox, Safari or formal assistive-technology certification. No live GitHub Pages deployment or live GA4-property receipt was claimed.

## Existing project to first software role — 3 September 2026

A moderated walkthrough with one final-semester student exposed a missing transition: she had a good CGPA, an independently completed final-year project and a software-development goal, but the site did not turn those assets into a practical internship or junior-role process. The new static bilingual guide is deliberately generic. Students can enter from project, presentation, interview, application or feedback bottlenecks and move through Target → Strengthen → Present → Practise → Apply → Improve. It treats CGPA as one supporting signal, ACM/ICPC as useful but optional evidence, and solo/team history honestly. Navigation, homepage, both situation flows, search, final-year/recovery guidance, employment goals and relevant careers/roadmaps now expose contextual entry points without adding a large homepage section, account, vacancy feed, stored progress or job guarantee.

| Check                                     | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dependency install                        | PASS; `yarn install --immutable` completed with Yarn 4.17.1. Windows prevented Corepack from rewriting an existing system shim, but this did not affect the pinned repository install or subsequent Yarn commands.                                                                                                                                                                                                                                                                              |
| Formatting, lint, type and content checks | PASS; `yarn format` and `yarn check` completed with 154 Astro/source files, zero errors, warnings or hints, and 44 unit/content tests in 13 files. New integrity assertions cover six stages, five entry anchors, bilingual copy, role/source relationships, discoverability and the honesty/CGPA/competitive-programming boundaries.                                                                                                                                                           |
| Root production build                     | PASS; `/` produced 187 HTML pages / 209 files with no build warning. `yarn verify:build` found no broken local URL, anchor, SEO or placeholder defect; `yarn inspect:build` reported compact one-line CSS/JS, six responsive WebP assets and zero source maps.                                                                                                                                                                                                                                  |
| Defect-and-rerun cycle                    | PASS after fixes. The first full root browser run passed 328 tests, intentionally skipped 106 duplicate-project cases and exposed ten failures from two causes: the new roadmap bridge inherited inaccessible link colors on a primary panel, and a new test used the wrong shared-header language marker. The CSS contrast and selector were corrected; the affected complete critical-route/first-role set then passed 111 tests with one intentional matrix skip on desktop/mobile projects. |
| GitHub Pages production suite             | PASS; `/CSE-Compass/` produced the same 187 HTML pages / 209 files, passed build verification/inspection, and the complete production E2E plus accessibility suite passed 338 tests with 106 intentional duplicate-project/viewport skips. The final `dist/` is this verified project-base output.                                                                                                                                                                                              |
| Responsive, theme and language            | PASS; automated coverage exercised 320, 390, 768, 1280 and 1600px; English/Bangla; light/dark; desktop/mobile projects; and the complete generated route frame audit. Live production visual review inspected the guide at 1280px and 390px, its sticky/native section index, English/Bangla switching, theme controls and the homepage bridge. Layout remained readable with no horizontal overflow or browser console warning/error; the temporary viewport was reset.                        |
| Accessibility                             | PASS; configured WCAG A/AA axe scans passed for the English/Bangla first-role routes in light and dark themes on desktop/mobile, together with all critical routes in the final complete suite. Keyboard navigation, real fallback links and no-JavaScript essential content passed. This is strong automated evidence, not formal certification or a screen-reader audit.                                                                                                                      |
| SEO and structured data                   | PASS at both bases; generated verification covers unique title/description/canonical, EN/BN/x-default hreflang, Open Graph, valid Article and BreadcrumbList JSON-LD, sitemap membership, robots sitemap URL, route/assets/anchor integrity and placeholder detection. Search remains `noindex, follow` and absent from the sitemap; the new guide is indexable and present in the sitemap.                                                                                                     |
| GA4                                       | PASS in the complete production suites using the local GA4 stub with Google requests blocked. Existing bounded navigation/source/action events—including the new contextual links—dispatch without leaking search text or adding personal data. Live GA4-property receipt was not tested.                                                                                                                                                                                                       |
| External resources                        | PASS with one review item; all 68 published learning/guidance URLs were checked after network access was enabled. 67 returned HTTP 200, including the new GitHub README/profile guidance and Microsoft interview guidance. Linux Journey returned HTTP 403 to automation and remains `REVIEW`; no URL was confirmed broken.                                                                                                                                                                     |
| Documentation and repository integrity    | PASS; README, DESIGN, AI-Prompt, SKILL, UX review and this record describe the first-role purpose, architecture, discovery model, safeguards and validation. `git diff --check` reported no whitespace defect; existing unrelated repository work was preserved.                                                                                                                                                                                                                                |

Genuine limitations: this finding came from one student and does not establish representative usability or employment outcomes. Browser checks use Chromium emulation rather than physical devices, Firefox, Safari or formal assistive-technology certification. No live GitHub Pages deployment, live GA4 receipt, employer-specific vacancy verification or job outcome was claimed. Linux Journey blocks the automated checker with HTTP 403.

## AI task-model desktop spectrum — 2 September 2026

The AI overview's task spectrum now uses the full section width at the desktop-sidebar breakpoint and keeps “Higher exposure → AI-assisted → responsibility-heavy” on one line. The type scales within a controlled desktop range; below `68rem`, normal wrapping returns so the phrase remains readable without causing horizontal overflow. English and Bangla share this responsive treatment.

| Check                                     | Result                                                                                                                                                                                                                                                                         |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Formatting, lint, type and content checks | PASS; `yarn format` and `yarn check` covered 148 files with zero Astro errors, warnings or hints, and all 41 unit/content tests passed.                                                                                                                                        |
| Browser geometry                          | PASS; Chromium measurements kept the English title to one line at 1088px and 1280px, while 390px wrapped naturally with zero horizontal overflow. Automated assertions covered English and Bangla at 1088, 1280 and 1600px plus 390px containment.                             |
| Root production regression                | PASS; 99 focused layout/accessibility tests passed with three intentional duplicate-project skips. Coverage includes the complete 184-route desktop-frame audit, the new task-spectrum assertion, desktop/mobile English/Bangla AI pages and light/dark AI accessibility.      |
| GitHub Pages regression                   | PASS; the same 99 focused tests passed with three intentional skips at `/CSE-Compass/`.                                                                                                                                                                                        |
| Accessibility                             | PASS; `/ai/` and `/bn/ai/` passed configured WCAG A/AA axe scans on desktop and mobile, and `/ai/` passed in dark theme on both projects.                                                                                                                                      |
| Production and SEO integrity              | PASS at the GitHub Pages base; 185 HTML pages / 206 files built without warnings. `yarn verify:build` found no broken local URLs or SEO/placeholder defects; `yarn inspect:build` confirmed minified one-line CSS/JS bundles, six responsive WebP assets and zero source maps. |

Genuine limitations: browser validation uses Chromium emulation rather than physical devices, Firefox, Safari or formal screen-reader certification. The final `dist/` is the verified `/CSE-Compass/` production output.

## Whole-site spacing and canonical creator portfolio — 2 September 2026

The initial audit checked the complete public route graph for horizontal overflow, content-frame containment and breadcrumb/hero alignment, but that metric was incomplete: it fixed the conditional blank column on **I'm Lost** without detecting that most other hero containers were technically full-width while their meaningful content remained stacked on the left. The follow-up review corrected the shared system. At `56rem` and above, every non-homepage hero now pairs its title with explanation, actions or context across the desktop frame; career and experiment details retain their existing purpose-built two-column compositions. Long titles use a controlled desktop display size, and the layout returns to unchanged single-column document flow below the breakpoint. The “I'm Lost” chooser still uses its complete initial width and restores a chooser/result split after selection.

The Amjad Hossain credit now points to the intended account-level GitHub Pages portfolio, `https://amjadhossainrahat.github.io/`, opens in a new tab and retains safe `rel` attributes. This canonical destination is intentional even before the separate portfolio is published. The build verifier now correctly treats same-host absolute URLs outside `/CSE-Compass/` as external GitHub Pages sites while continuing to reject root-relative links that escape the project base.

| Check                                     | Result                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type and content checks | PASS; `yarn check` covered 148 files with zero Astro errors, warnings or hints, and all 41 unit/content tests passed.                                                                                                                                                                                                                                                                               |
| Root production browser suite             | PASS; 316 tests passed with 104 intentional duplicate project/viewport skips. Coverage includes complete E2E and accessibility suites, English/Bangla, light/dark, keyboard/no-JavaScript flows, GA4 event bounds, downloads and console/page-error capture.                                                                                                                                        |
| Whole-site desktop spacing                | PASS at both `/` and `/CSE-Compass/`; the strengthened audit visited all 184 public English/Bangla UI routes at 1280px and found no overflow, frame escape or breadcrumb/hero misalignment. It now also requires meaningful content to originate in both hero columns on every applicable page. The I'm Lost state-transition width assertion passed before and after a selection.                  |
| Focused GitHub Pages regression           | PASS; 92 tests passed with two intentional duplicate mobile-project skips. Coverage included the exhaustive two-column hero audit and critical WCAG A/AA axe checks across desktop/mobile, both languages and both themes.                                                                                                                                                                          |
| Responsive/theme/language                 | PASS; the full root suite exercised 320/390/768/1280/1600px, English/Bangla and light/dark. Fresh visual review covered the English final-year project, long software-foundations and goal-index heroes at 1440px, the Bangla final-year hero at 1280px and its unchanged single-column flow at 390px. All reviewed pages had zero horizontal overflow; console/page-error coverage remained clean. |
| Root and project-base builds              | PASS; both bases produced 185 HTML pages / 206 files without build warnings. The final `/CSE-Compass/` output passed `yarn verify:build`; `yarn inspect:build` reported minified one-line CSS/JS bundles, six responsive WebP assets and zero source maps.                                                                                                                                          |
| SEO                                       | PASS for that revision; generated output verified unique titles/descriptions/canonicals, EN/BN/x-default hreflang, Open Graph and image alt metadata, valid JSON-LD, sitemap membership/exclusions, robots sitemap URL, local routes/assets/anchors and unfinished-copy detection. Its then-existing Search routes were `noindex, follow`; the newer 9 September revision above removed them.       |
| External resources                        | PASS with one review item; the latest link check returned HTTP 200 for 65 links. Linux Journey returned HTTP 403 to automation and remains marked `REVIEW`; no link was confirmed broken. The intentionally not-yet-published creator portfolio is documented rather than misrepresented as live.                                                                                                   |
| Documentation                             | PASS; README, DESIGN, AI-Prompt, SKILL, UX review and this record agree on desktop spacing behavior, whole-route regression coverage and the canonical creator portfolio URL.                                                                                                                                                                                                                       |

Genuine limitations: browser validation uses Chromium emulation, not physical devices, Firefox, Safari or formal screen-reader certification. No real-student usability study, live deployment or live GA4-property receipt was performed. The creator portfolio URL is structurally correct but may return GitHub Pages' not-found response until that separate site is published. Linux Journey blocks the automated link checker with HTTP 403.

## Specialist careers, ACM practice track and third inspiration — 1 September 2026

Expanded the landscape from fourteen to seventeen complete careers by adding Data Analytics & BI, AI Engineering and Application Security to their existing families. Each has bilingual work reality, comparison dimensions, five-stage role-specific roadmap, safe starter experiment, AI task exposure, curated resources and connections to all seven goal guides. The homepage still presents three fast experiment choices and reveals the three expanding paths through a keyboard/native disclosure. Understand Computing Deeply now contains a separate ACM Programming / ICPC practice-track callout, leading to a six-week deliberate-practice loop and a scoped Bangla shelf for Shafaet's Planet and Amirul Islam. Amirul Islam is also the third visible inspiration in the shared footer and About credits.

| Check                              | Result                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint and type checking | PASS; final `yarn check` covered 140 files with zero Astro errors, warnings or hints.                                                                                                                                                                                                                                                                                                                                                               |
| Unit and content integrity         | PASS; 40 tests in 12 files. Assertions cover all 17 stable career IDs, exact family membership, specialist AI exposure, role-specific roadmaps, five-step experiments, all seven goal connections and three inspiration URLs.                                                                                                                                                                                                                       |
| End-to-end journeys                | PASS at `/CSE-Compass/`; 190 tests passed with 102 intentional duplicate-matrix skips. Coverage includes all specialist career/roadmap/experiment/AI routes in English and Bangla, homepage disclosure, ACM link, goals, comparison, keyboard/no-JavaScript use, GA4 event bounds and download behavior. An initial run exposed the stale 14-card assertion and extension-route download URL; both were fixed and the complete suite reran cleanly. |
| Accessibility                      | PASS; 108 tests across desktop/mobile projects, English/Bangla and light/dark themes found no configured WCAG A/AA axe violations. New coverage includes AI Engineering, Application Security, Data Analytics & BI and both localized competitive-programming guides. This is not formal certification or a complete assistive-technology audit.                                                                                                    |
| Responsive/theme/language          | PASS; automated 320/390/768/1280/1600px matrices found no overflow or console/page error. Manual browser review covered English desktop dark homepage/family/disclosure views and Bangla mobile dark homepage, family practice track, ACM guide and Bangla resource shelf. The temporary viewport was reset and no browser warning/error was present.                                                                                               |
| Root and project-Pages builds      | PASS; `/` and `/CSE-Compass/` each produced 183 HTML pages / 204 files with no build warnings. `yarn verify:build` passed at both bases. Final project output remains in `dist/`.                                                                                                                                                                                                                                                                   |
| SEO                                | PASS at both bases; generated verification covers unique titles/descriptions/canonicals, EN/BN/x-default hreflang, Open Graph, parseable JSON-LD, sitemap membership, robots sitemap URL, local routes/assets/anchors and placeholder detection. Dynamic career, roadmap, experiment and AI routes include the three new careers automatically.                                                                                                     |
| Production assets                  | PASS; `yarn inspect:build` reports four one-line minified JavaScript bundles, four one-line minified CSS bundles, six responsive WebP files, zero source maps and byte-for-byte public assets. Markdown downloads intentionally remain readable and hosting compression is not claimed.                                                                                                                                                             |
| External learning resources        | PASS with limitation; 65 of 66 links returned HTTP 200, including both user-supplied Bangla sources and the Amirul AI roadmap. Linux Journey returned HTTP 403 and is correctly marked `REVIEW`; there were no confirmed broken links.                                                                                                                                                                                                              |
| Documentation                      | PASS; README, DESIGN, AI-Prompt, SKILL, UX review and this validation record describe the 17-career graph, progressive experiment preview, ACM/ICPC practice track and three inspirations.                                                                                                                                                                                                                                                          |

Genuine limitations: testing used Chromium desktop/mobile emulation rather than physical devices, Safari or Firefox. Axe and keyboard checks are not formal screen-reader certification. External resources can change after review; Linux Journey blocks the automated checker. No live deployment or production GA4 delivery was performed.

## Home navigation, creator link and mobile menu — 1 September 2026

Historical note: this record describes the destination selected during that validation run. The 2 September 2026 decision above supersedes it; the current canonical creator link is `https://amjadhossainrahat.github.io/`, including before that separate portfolio is published.

Added an explicit bilingual Home item to the shared Start here group. Home alone receives `aria-current="page"` at the locale homepage; the separate “Find my next step” anchor remains useful without presenting two current destinations. The phone/tablet native-menu summary now displays only the hamburger icon while retaining the localized `Menu` / `মেনু` accessible name and 44px target. Public creator credit is now “Amjad Hossain” in metadata, About, footer, documentation and license. The visible name links in a new tab with `noopener noreferrer` to the verified GitHub profile at `https://github.com/AmjadHossainRahat`.

| Check                              | Result                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint and type checking | PASS; final `yarn format` and `yarn check` completed with 137 source files and zero errors, warnings or hints.                                                                                                                                                                                                                                                                                                            |
| Unit and content-integrity tests   | PASS; 38 tests in 12 files. New assertions protect the Home route, single current-page state, exact creator name and configured creator URL. The first run exposed an overly strict Bangla-label-length assertion for the valid three-character “হোম”; the assertion was corrected and the full check reran cleanly.                                                                                                      |
| Root and project production builds | PASS; final builds at `/` and `/CSE-Compass/` each produced 159 HTML pages / 180 files with no build warning. `yarn verify:build` passed at both bases and confirms the creator/profile credit in every generated footer. Final project output also passed `yarn inspect:build`: minified bundles, six responsive WebP assets, zero source maps and byte-for-byte public assets.                                          |
| Production browser regression      | PASS; the affected production suite passed 60 tests with 20 intentional duplicate-matrix skips at each base. Coverage included credits, homepage/navigation, no-JavaScript, keyboard, English/Bangla, light/dark, mobile and desktop projects, and 320/390/768/1280/1600px credit layouts with clean console/page-error capture. After correcting the external target, a final three-test project-base rerun passed.      |
| Accessibility                      | PASS; 16 focused accessibility cases passed at each base across English/Bangla, light/dark and desktop/mobile projects. Axe checks covered the homepage entry/navigation state, About credit and fresher-page footer; keyboard and no-JavaScript credit/navigation paths also passed. This does not establish formal WCAG certification.                                                                                  |
| Visual and live-browser review     | PASS; screenshots were inspected at English light 320px, Bangla dark 390px and English light 1280px. The icon-only toggle, Home placement, credit wrapping and sidebar alignment were clean. Live 390×844 checks confirmed `☰` as the only visible toggle text, localized accessible names, Home locale URLs/current state, no competing next-step state and no console warning/error; the temporary viewport was reset. |
| Creator destination                | PASS after correction. The initially inferred GitHub Pages root from `SITE_URL` opened a separate tab but returned “Site not found.” It was not shipped as the credit. The public `AmjadHossainRahat` GitHub profile was independently opened, matched the repository owner and loaded successfully; the final credit uses that verified destination with `target="_blank"` and safe `rel` attributes.                    |
| SEO and GitHub Pages readiness     | PASS at both bases: title/description/canonical, EN/BN/x-default hreflang, Open Graph, JSON-LD, sitemap, robots sitemap URL, local URLs/assets/anchors and placeholder checks remain clean. All new internal Home URLs honor the configured base, and the final `dist/` is the `/CSE-Compass/` production output.                                                                                                         |

Limits: focused browser coverage used Chromium desktop/mobile emulation, not physical devices, Firefox, Safari or formal screen-reader certification. The GitHub profile was verified live, but no separate deployed portfolio site was discoverable; the stable public GitHub profile is therefore the creator destination.

## Intent-based sidebar and mobile drawer — 1 September 2026

Replaced the crowded wide-screen top navigation with a persistent 16.75rem labeled sidebar and reorganized global destinations by student intent: Start here, Discover, Prepare, Future and About. Phones and tablets retain a compact sticky header and receive the same bilingual groups in a viewport-bounded native drawer. The current section uses `aria-current="page"`, border and background—not color alone. “New to CSE,” “I’m Lost,” goal preparation, the final-year guide and “Why CSE Compass?” are now globally discoverable. `src/data/navigation.ts` is the typed source for labels, routes, emphasis and exact/prefix matching; `Header.astro` supplies both layouts without duplicating links or adding a framework. The footer now focuses on project purpose, final-year guidance, privacy, repository and required inspiration/collaboration credits instead of duplicating career navigation. README, DESIGN, AI-Prompt, SKILL and the UX review were synchronized.

| Check                                      | Result                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint and type checking         | PASS; `yarn format` and `yarn check`, 137 files with zero errors, warnings or hints.                                                                                                                                                                                                                                                   |
| Unit/content integrity                     | PASS; 38 tests in 12 files, including bilingual group completeness, unique item IDs and exact/prefix active-route behavior.                                                                                                                                                                                                            |
| Project-base full production browser suite | PASS; `/CSE-Compass/`, 284 tests passed and 102 intentional duplicate-viewport/device skips out of 386.                                                                                                                                                                                                                                |
| Root full production browser suite         | PASS; `/`, the same 284 passed / 102 intentional skips.                                                                                                                                                                                                                                                                                |
| Accessibility                              | PASS; 94 accessibility tests and 110 axe scans per full base-path run. Coverage includes critical routes, both themes, English/Bangla, the expanded mobile drawer, homepage situations, About/footer and fully expanded project workflows. No detectable configured WCAG A/AA violation.                                               |
| Responsive navigation and visual review    | PASS; 320×720, 390×844, 768×1024, 1280×800 and 1600×900 across English/Bangla and light/dark. Tests verify drawer open/close, Escape and keyboard focus, no-JavaScript native access, breakpoint continuity, active route, exact sidebar/frame separation, drawer scrolling, no horizontal overflow and clean console/page-error logs. |
| Root production build                      | PASS; 159 HTML pages / 180 files, no warnings; `yarn verify:build` found no broken local URL, anchor, metadata or placeholder defect.                                                                                                                                                                                                  |
| GitHub Pages production build              | PASS; `/CSE-Compass/`, 159 HTML pages / 180 files, no warnings and clean `yarn verify:build`.                                                                                                                                                                                                                                          |
| SEO/output integrity                       | PASS at both bases: unique titles/descriptions/canonicals, EN/BN/x-default hreflang, Open Graph, valid JSON-LD, sitemap, robots sitemap URL, local links/assets/anchors and unfinished-copy checks. Navigation remains static internal linking on every generated page.                                                                |
| Analytics                                  | PASS in both full suites with the local GA4 stub and Google requests blocked. Existing bounded events still dispatch; no new navigation tracking or personal data was added. Live-property receipt was not tested.                                                                                                                     |
| Production assets                          | PASS via `yarn inspect:build`; existing compact bundle structure retained, six responsive WebP variants, zero source maps and byte-for-byte public assets. The redesign adds no dependency, route, React hydration or new JavaScript bundle.                                                                                           |

Visual inspection covered the English-light 1600px sidebar, Bangla-dark 1280px sidebar, English-light 320px drawer, Bangla-dark 390px drawer and Bangla-dark 768px two-column drawer. Labels wrap cleanly, the page starts exactly after the sidebar, theme/language controls remain visible, the active item is unmistakable and every teammate remains visible in the hero. The 320px drawer correctly scrolls within the viewport to reach later groups rather than expanding the document sideways.

The first focused browser run reported five failures. Four no-JavaScript artwork checks had left the intentionally server-open native menu covering the page; those tests now close the menu before interacting with the hero. One selector matched both the new purpose link and its required footer counterpart; it now targets the navigation item explicitly. The corrected focused run passed 29 tests with one intentional desktop/mobile skip, the responsive matrix passed 21/21, and both subsequent full production suites passed. No application defect was hidden or coverage removed.

Limits: browser validation uses Chromium desktop/mobile emulation rather than physical devices, Firefox, Safari or formal screen-reader certification. Axe and keyboard review do not establish complete WCAG conformance. No real-fresher usability study, live GA4 receipt, deployed GitHub Pages run or HTTP-compression verification is claimed. External learning-resource URLs were unchanged and were not rechecked for this navigation-only revision.

## Origin story, purpose and recovery path — 1 September 2026

Turned the creator's origin story into a two-layer experience. The homepage now explains the attraction-to-orientation gap in a short, non-judgmental arc beneath the established “You got into CSE. Now what?” / “CSE-তে ঢুকেছ। এখন কী?” headline, reassures students that direction can still be rebuilt and immediately offers the existing next steps. The About page carries the complete version as a four-stage visual sequence—Attraction, Orientation gap, Drift and frustration, Direction—followed by explicit Purpose and Goal panels and a note supporting independent or orientation-class use without implying institutional endorsement. Typed English/Bangla copy lives in the shared `sitePurpose` data rather than duplicated page markup. The wording avoids unsupported prevalence claims, blame and the fatalistic “missed the train” metaphor; it presents familiar paths such as software engineering, QA, competitive programming and research as valid examples, not the whole field. README, DESIGN, AI-Prompt and SKILL were synchronized.

| Check                               | Result                                                                                                                                                                                                                                                                                  |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint and type checking  | PASS; `yarn format` and `yarn check`, 136 files with zero diagnostics.                                                                                                                                                                                                                  |
| Unit/content integrity              | PASS; 36 tests in 12 files, including bilingual completeness, Bangla-script and narrative/recovery assertions.                                                                                                                                                                          |
| Project-base production browser run | PASS; 79 tests with 41 intentional duplicate-viewport skips across the affected homepage, About/credits and accessibility suites.                                                                                                                                                       |
| Anchor/screenshot regression rerun  | PASS; 24 Chromium About tests after making the test wait for native smooth scrolling to settle.                                                                                                                                                                                         |
| Root production build               | PASS; `/`, 159 HTML pages / 180 files, no build warnings; `yarn verify:build` found no broken local URL, SEO or placeholder defect.                                                                                                                                                     |
| GitHub Pages production build       | PASS; `/CSE-Compass/`, 159 HTML pages / 180 files, no build warnings; `yarn verify:build` passed.                                                                                                                                                                                       |
| Production assets                   | PASS via `yarn inspect:build`; four compact JavaScript bundles, four compact CSS bundles, six responsive WebP variants, zero source maps and byte-for-byte public assets. This content change added no route, dependency or client bundle.                                              |
| Accessibility                       | PASS in the focused production-preview run for homepage entry and About/credits across English/Bangla and light/dark; axe, keyboard and no-JavaScript assertions detected no regression. This remains automated evidence, not a complete WCAG certification.                            |
| Responsive/theme/language           | PASS; automated 320, 390, 768, 1280 and 1600px matrices across English/Bangla and light/dark reported no overflow, console error or page error. English-light 320px and Bangla-dark 1280px settled story captures were visually inspected for hierarchy, wrapping and anchor alignment. |
| SEO/output integrity                | PASS at both bases: titles/descriptions, canonical URLs, EN/BN/x-default hreflang, Open Graph, valid JSON-LD, sitemap, robots sitemap URL, local links/assets/anchors and unfinished-copy checks.                                                                                       |

During validation, an early source placement mistake put the story in the wrong conditional page branch; source inspection caught it before the recorded checks, and the final section is only on About. The first responsive screenshot could also catch the page mid-way through smooth scrolling; the test now waits for the target to align with the document scroll padding, and all 24 affected Chromium checks passed afterward. The final generated artifact is the `/CSE-Compass/` build.

Limits: this focused revision did not rerun the unchanged external-resource link checker, verify delivery to a live GA4 property, deploy GitHub Pages or conduct a real-student usability study. Browser evidence uses Chromium emulation rather than physical devices, Safari, Firefox or screen-reader certification. Earlier complete-suite and GA4 records below remain historical evidence, not newly repeated claims.

## Purpose-first homepage and clearer entry journeys — 1 September 2026

Reworked the opening around audience, purpose, practical outcomes and one useful next step. Three primary intentions link directly to the fresher guide, career index and goal hub; all six previous situations remain in a native disclosure. Question-led section introductions and an explicit roadmap/goal distinction support onward exploration. Preserved AI immediately after the hero, inclusive artwork, eight career families, three experiment previews, all six useful-loop links, encouragement and complete detail pages. Typed English/Bangla introduction and entry data live in `src/data/homepage.ts`; no new route, dependency, framework, client script or persistent state was added. README, DESIGN, AI-Prompt, SKILL and the UX review were synchronized.

Follow-up wording adjustment: restored the more memorable hero punch line “You got into CSE. Now what?” / “CSE-তে ঢুকেছ। এখন কী?” while retaining the purpose-first eyebrow, benefit explanation, reassurance and three-entry flow. After this adjustment, `yarn check` passed with 136 files and all 35 unit/content tests; the `/CSE-Compass/` production build again passed at 159 HTML pages / 180 files with no warnings or output-verification defects. A focused production-preview run passed 64 homepage/journey/accessibility tests with 22 intentional duplicate-viewport skips. Fresh English-light 320px and Bangla-dark 1280px screenshots were visually inspected; the headline, primary action, artwork and orientation map remained readable and aligned.

| Check                                   | Result                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `yarn install --immutable`              | PASS; Yarn 4.17.1, unchanged dependencies/lockfile. Initial cache permission denial resolved through the approved cache-access retry.                                                                                                                                                                                                                                  |
| `yarn format`, `yarn check`             | PASS; formatting and lint, 136 checked files with zero type errors/warnings/hints.                                                                                                                                                                                                                                                                                     |
| Unit/content integrity                  | PASS; 35 tests in 12 files: 15 unit and 20 content tests, including three new homepage data/route/parity checks.                                                                                                                                                                                                                                                       |
| Project-base production build           | PASS; `/CSE-Compass/`, 159 HTML pages / 180 files; no build warnings.                                                                                                                                                                                                                                                                                                  |
| Root production build                   | PASS; `/`, 159 HTML pages / 180 files; no build warnings.                                                                                                                                                                                                                                                                                                              |
| Project-base full E2E/accessibility run | PASS; 282 tests, 102 intentional device/viewport skips. No homepage exclusion or grep filter.                                                                                                                                                                                                                                                                          |
| Root full E2E/accessibility run         | PASS; 282 tests, the same 102 intentional skips. Includes the corrected scroll-settling assertion.                                                                                                                                                                                                                                                                     |
| Project-base responsive rerun           | PASS; 21 Chromium tests after improving screenshot timing; no skips.                                                                                                                                                                                                                                                                                                   |
| Accessibility                           | PASS; 94 accessibility tests per full run, including 102 axe scans, both languages/themes, closed/open homepage situations and fully expanded project workflows. No detected violations.                                                                                                                                                                               |
| Responsive/theme/language               | PASS; explicit 320×720, 390×844, 768×1024, 1280×800 and 1600×900 matrix × English/Bangla × light/dark. Primary action fully inside the initial viewport; introduction precedes action, mobile copy precedes artwork; no horizontal overflow.                                                                                                                           |
| Journeys and keyboard/no-JavaScript     | PASS; all three primary entries reach real guide content, all six detailed situations retain their localized destinations, native disclosure and entry routes work by keyboard with JavaScript disabled. Existing career, goal, comparison, roadmap, experiment and project-toolkit regressions pass.                                                                  |
| GA4 integration                         | PASS with local stubs and Google requests blocked; hero, three entry destinations, original situation events and existing page/resource events dispatch bounded payloads. Live-property receipt was not tested.                                                                                                                                                        |
| SEO and local output integrity          | PASS at both bases via `yarn verify:build`: titles/descriptions, unique canonicals/titles, EN/BN/x-default hreflang, Open Graph image, valid JSON-LD, sitemap coverage, robots sitemap URL, local links/assets/anchors and placeholder checks. Both generated homepage metadata sets were inspected directly, including OG/X descriptions and WebSite structured data. |
| Production assets                       | PASS via `yarn inspect:build` at both bases: four one-line JS bundles, four one-line CSS bundles, six responsive WebP variants, zero source maps and unchanged byte-for-byte public assets. No new JS bundle; inline scripts/public assets/readable Markdown retain their documented exceptions.                                                                       |
| Console and source hygiene              | PASS; browser error logs and responsive console/page-error assertions clean; `git diff --check` and source/public placeholder scan clean.                                                                                                                                                                                                                              |

### Reproduction and test accounting

For each base, set `BASE_PATH` to `/CSE-Compass/` or `/`, run `yarn build`, `yarn verify:build` and `yarn inspect:build`, then set `PLAYWRIGHT_SERVER=preview` and run:

```text
yarn playwright test tests/e2e tests/accessibility
```

Each full run has 188 passing E2E tests and 94 passing accessibility tests. The 102 skips are 101 duplicate explicit-viewport cases under the mobile project plus one mobile-only navigation case under the desktop project; the corresponding real coverage runs in the appropriate project. Skips are not counted as passes. The additional project-base command was `yarn playwright test tests/e2e/responsive-and-console.spec.ts --project=chromium`.

### Visual review and fixes

Inspected real viewport captures: English light and Bangla dark 320px hero, Bangla dark 390px entry choices, Bangla light 768px hero/entry, English light 1280px hero/entry, and English dark 1600px hero/map. Reviewed text wrapping, readable hierarchy, action visibility, complete artwork and aligned map captions. Initial entry screenshots caught a native smooth scroll partway through; the responsive test now waits until the target aligns with the document's scroll padding. Reran all 21 project-base responsive checks and then the full root suite with this stronger assertion. No application defect or failed check was hidden by removing coverage.

The supported in-app Browser skill also exercised the updated homepage, primary action, actual fresher destination, equivalent-language navigation, theme toggle, expanded Bangla situations and career-landscape anchor. Direct homepage access succeeded on this revision; the earlier historical access limitation does not describe this run. A manual selector initially used the English theme label after switching languages; reading the translated control and using its Bangla label resolved the selector, without changing the application. Browser error logs were empty. When restoring the preview, the direct Astro command initially attempted to create a denied telemetry configuration directory; using the same `ASTRO_TELEMETRY_DISABLED=1` setting as the repository scripts resolved startup. The final local preview uses `/CSE-Compass/`.

Limits: Chromium desktop/mobile emulation, not physical devices, Safari/Firefox or a screen-reader certification. Automated axe plus keyboard/visual checks are not proof of complete WCAG conformance. No real-fresher usability study, conversion improvement, live GA4 receipt, deployed Pages run or remote branch-protection verification is claimed. External resource URLs were unchanged and not rechecked in this homepage revision; earlier link-check results remain below. Broader navigation/search/“I'm Lost” findings in the UX review are outside this authorized homepage change. No push, deployment or additional file deletion was performed.

Final preview caveat: a navigation during the initial failed preview startup produced a connection-error page. After startup was fixed, the in-app browser tool blocked navigation on that error page's `data:` URL. No alternate browser or URL workaround was attempted. Astro's status and logs independently confirm the project-base preview running and ready, but the final in-app reload is not claimed as successful. This occurred after the successful manual review and both complete production-preview test runs, and does not invalidate those earlier checks.

## Visible credits, fresher UX audit and safe cleanup — 1 September 2026

Added a compact shared-footer acknowledgment and localized About `#credits`, including the two inspiration sites required at that stage and the distinct Creator & Orchestrator, ChatGPT and Codex contributions. Typed bilingual credit data avoids duplicating names/roles between views. About metadata and direct jump links were updated. The later specialist-career expansion added Amirul Islam as a third inspiration through the same model. No new dependency, client script, analytics event or homepage body section was added. README, DESIGN, AI-Prompt, SKILL and illustration provenance were synchronized. [ux-review.md](ux-review.md) records source evidence and prioritized recommendations; these recommendations are not an implemented navigation redesign or a real-user study.

Removed only the obsolete tracked `src/assets/illustrations/mentor-guide.png` (2,058,944 bytes) after checking imports/references and Git recoverability. The current hero/robot masters, theme reference, source entry points, scripts, tests and documentation remain. A supplementary filename-reference scan across 133 code/style/test/script files found no further unreferenced source candidates after excluding framework entry points; that heuristic is not a proof of complete dead-code elimination. Generated output and dependencies remain ignored and untracked. The deleted asset was already excluded from production, so no transfer-size reduction is claimed.

| Check                                      | Result                                                                                                                                                                                                                                                                |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `yarn install --immutable`                 | PASS; Yarn 4.17.1, unchanged lockfile/dependencies. The initial cache permission denial was resolved with the approved cache-access retry.                                                                                                                            |
| `yarn format`, `yarn check`                | PASS; formatting, lint, 132 checked files with zero type errors/warnings/hints.                                                                                                                                                                                       |
| Unit/content tests                         | PASS; 32 tests across 11 files: 15 unit and 17 content, including 3 new attribution-integrity checks.                                                                                                                                                                 |
| Root production build/output               | PASS; 159 HTML pages / 180 files, route/assets/metadata/placeholder verification and visible footer credits on every HTML page.                                                                                                                                       |
| `/CSE-Compass/` production build/output    | PASS; 159 HTML pages / 180 files; no build warnings; correct localized footer-to-About links and target anchors.                                                                                                                                                      |
| Root production-preview credits tests      | PASS; 36 tests, 20 intentional duplicate-viewport skips. Includes 28 E2E and 8 accessibility tests (16 axe scans over About and fresher pages).                                                                                                                       |
| Project-base production-preview regression | PASS; 104 tests, 20 intentional duplicate-viewport skips: 34 E2E and 70 accessibility tests (78 axe scans). Includes non-homepage critical routes and existing career/language/comparison flows.                                                                      |
| Responsive/localized/themed credits        | PASS; 320, 390, 768, 1280, 1600px × English/Bangla × light/dark, no horizontal overflow or console/page errors; footer credit links meet the asserted 44px minimum height.                                                                                            |
| Keyboard/no-JavaScript credits             | PASS; native anchor activation, visible focus, static credit content and equivalent-language navigation tested without adding hydration.                                                                                                                              |
| SEO                                        | PASS at both bases; unique titles/descriptions/canonicals, EN/BN/x-default hreflang, Open Graph, valid JSON-LD, sitemap coverage, robots sitemap URL, local routes/assets and same-page anchors. About EN/BN canonical/OG/X metadata also inspected directly.         |
| Production assets                          | PASS; `yarn inspect:build` at both bases found four compact JS bundles, four CSS bundles, six responsive WebP assets, no source maps and unchanged byte-for-byte public assets. Existing inline-script/public/Markdown exceptions still apply.                        |
| External inspiration sites                 | roadmap.sh opened successfully; Architect Prep's live page could not be independently inspected because the research browser declined the URL. Its original supplied link is preserved and rendered correctly, not claimed as live-verified. No workaround attempted. |
| Diff/source hygiene                        | PASS; `git diff --check`; source placeholder search found only test assertions that reject placeholders, no unfinished source copy.                                                                                                                                   |

### Reproduction and scope

After a root build and `BASE_PATH=/`, the credits run used `PLAYWRIGHT_SERVER=preview` with:

```text
yarn playwright test tests/e2e/credits.spec.ts tests/accessibility/credits.spec.ts
```

After the project-base build and `BASE_PATH=/CSE-Compass/`, the expanded run used:

```text
yarn playwright test tests/e2e/credits.spec.ts tests/accessibility/credits.spec.ts tests/e2e/core-flows.spec.ts tests/accessibility/critical-routes.spec.ts --grep-invert 'homepage|theme switching|starting point and lost flow|mobile navigation|violations: /(bn/)?$'
```

The homepage browser navigation that was previously blocked was not retried or reached through another browser surface. Its source and generated HTML were reviewed, including the shared footer. The filtered run is intentionally **not** described as a full-site browser rerun. The 20 reported skips avoid rerunning the explicit viewport matrix under the mobile device project; excluded homepage tests are not counted as passes.

### Visual and interactive review

Inspected actual viewport screenshots: English desktop light About/footer; Bangla dark 390px About/footer; Bangla light tablet footer; English dark 320px footer. Credit links, attribution hierarchy, wrapping and theme contrast were readable. The compact credit block sits below student-navigation links rather than competing with the hero. Automated checks also exercised the wider 1600px layout.

The supported in-app Browser skill was used for direct About navigation, the credits jump, theme switching, equivalent English-to-Bangla navigation and the 390px dark view. The inspected browser error logs were empty. This manual review stayed on the supported browser surface. No screen-reader, physical-device, Safari/Firefox, real-fresher usability or Lighthouse-score claim is made.

The unchanged GA4 payload unit tests passed, but the homepage GA4 integration test and live-property delivery were not rerun. Unchanged external learning-resource URLs were not rechecked. No deployment, push, branch protection change or history rewrite was performed. The `/CSE-Compass/` preview was restored after test teardown. Broader wayfinding and existing “I'm Lost” resilience findings remain explicitly documented recommendations, not silently claimed fixes.

## Practical final-year team workflows — 1 September 2026

Added seven bilingual working playbooks and ten reusable template previews/download sections while retaining all seven delivery stages and four project-type adaptations. Coverage includes task-manager setup, vertical slices, Ready/Done, WIP/blockers, day-one repositories, safe Git collaboration, async/sync routines, risk-based tests, defect reports, CI and release/handover. Template fields have one typed source for the page and generated Markdown. No new browser dependency, hydration, persistent state or homepage section was added. README, DESIGN, AI-Prompt and SKILL were updated; `docs/engineering-audit.md` records the folder/minification audit and `yarn inspect:build` makes artifact inspection repeatable.

| Check                                      | Result                                                                                                                                                                                                                          |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `yarn install --immutable`                 | PASS; Yarn 4.17.1, no dependency/lockfile changes. Initial user-cache sandbox denial was resolved by the approved cache-access retry.                                                                                           |
| Formatting, lint, type checking            | PASS; `yarn check`, 128 checked files, zero type errors/warnings/hints.                                                                                                                                                         |
| Unit tests                                 | PASS; 15 tests across 7 files, including deterministic complete localized toolkit rendering.                                                                                                                                    |
| Content integrity                          | PASS; 14 tests across 3 files, including all seven playbooks, ten templates, bilingual fields, valid source relationships and connected worked examples.                                                                        |
| Root production-preview regression suite   | PASS; 86 tests, 40 intentional duplicate viewport skips. This is the final-year/infrastructure subset, not a full-site browser rerun.                                                                                           |
| Root and `/CSE-Compass/` production builds | PASS; 159 HTML pages / 180 files each. Build output inspected; no warnings.                                                                                                                                                     |
| Root/project generated output              | PASS; all local routes/assets/anchors and metadata checks, including localized titles/descriptions, canonicals, EN/BN/x-default hreflang, Open Graph, valid JSON-LD, sitemap and robots sitemap URLs.                           |
| Asset/minification audit                   | PASS; four compact JS and four CSS bundles, six responsive WebP files, no emitted source maps, two byte-for-byte public assets. Inline/public/readable-Markdown exceptions documented; no HTTP compression claim.               |
| External resource links                    | 53 verified HTTP 200; Linux Journey returned HTTP 403 and remains REVIEW, not a verified success. Initial sandbox-wide network failures were rerun with approved network access. All four new official references returned 200. |

The root suite includes 70 E2E checks and 16 axe checks: closed and fully expanded final-year content, English/Bangla and light/dark under desktop/mobile projects. It exercises native disclosures, task links, all template previews, HTTP downloads and real browser downloads, equivalent-language navigation, no-JavaScript keyboard use, infrastructure-career relationships and comparison, and local-stub analytics without contacting Google. Explicit 320/390/768/1280/1600px matrices cover the guide and infrastructure pages; they run once rather than duplicating under the mobile device project. Console/page-error assertions are clean. No skipped defect is counted as a pass.

Visual inspection of generated screenshots covered desktop English workflow entry/navigation, expanded Git steps, Bangla small-mobile text wrapping and Bangla tablet dark template fields. Additional screenshot framing was improved to capture section headings and closed-overview choices, rather than only the middle of a long expanded article. This is actual screenshot review, not just overflow assertions.

### Project-base and interactive review

The `/CSE-Compass/` production-preview subset also passed all 86 tests, with the same 40 deliberate duplicate viewport skips and 16 axe checks. Both base-path runs used the final corrected content. The local project-base preview was restored after test teardown. The unchanged homepage URL navigation previously blocked by browser policy was not retried; the newly requested final-year guide was accessible through the normal in-app browser.

Direct in-app-browser review confirmed desktop workflow choices/section navigation, disclosure interaction and dark styling, equivalent-guide English-to-Bangla switching, and the expanded Bangla test-plan template at 390px with visible keyboard focus and readable wrapping. The inspected browser error logs were empty. One manual selector initially included the wrong whitespace around a visually hidden label; correcting the selector allowed the existing native disclosure to open, with no application defect or workaround. The Browser skill kept this review on the supported in-app surface.

### Fixes during validation

- Replaced two English-only long field labels in the Bangla toolkit with natural Bangla prompts; kept the bilingual integrity rule intact.
- Added the explicit Node Buffer import required by lint in the read-only asset inspector.
- Resolved the Astro formatter’s initial second-pass change, then reran formatting and the complete non-browser suite.
- Rebuilt and reran the root production subset after the final text correction; earlier successful output was not treated as validation of the corrected artifact.

Browser coverage here is Chromium with desktop/mobile emulation, not Safari/Firefox, physical devices or assistive-technology certification. Axe and keyboard/visual checks do not prove full WCAG conformance. Unchanged homepage/goal journeys were not rerun in this scoped revision. No live GA4-property receipt, live Pages deployment, compression headers, remote branch protection, Git push or hosting changes are claimed. Earlier dated records below retain their original scope and limitations.

## Earlier records — 31 August 2026

## Three-person computing team with a Chakma QA teammate

Added an original fictional Chakma Bangladeshi woman between the two existing laptop users using one built-in ImageGen edit. Her checklist tablet and stylus show active QA participation. All three have visitor-facing smiles and equal prominence; the seated woman's teal hoodie and fully hair-covering ivory orna, both laptops, technical notebook and desk props remain. The contemporary shoulder cloth is textile-inspired, with consulted sources and authenticity limits recorded in `docs/illustrations.md`. English/Bangla alt text and its normal/no-JavaScript regression assertions now describe the three-person scene. README, DESIGN, AI-Prompt and SKILL were updated. No third language, new dependency, carousel or client script was added.

| Check                                      | Result                                                                                                                                                                                                                                                                       |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type checking            | PASS; `yarn check`, 119 type-checked files, zero errors, warnings or hints.                                                                                                                                                                                                  |
| Unit/content integrity                     | PASS; 25 tests across 9 files (13 unit, 12 content).                                                                                                                                                                                                                         |
| Root production regression suite           | PASS; 105 tests and 21 intentional duplicate skips: homepage, responsive/console and all 70 accessibility checks.                                                                                                                                                            |
| Root and `/CSE-Compass/` production builds | PASS; 159 HTML pages and 179 files each; no build warnings.                                                                                                                                                                                                                  |
| Root/project output verification           | PASS; localized metadata, canonical URLs, English/Bangla/x-default hreflang, Open Graph, structured data, sitemap, robots, internal routes/assets/anchors and unfinished-copy checks.                                                                                        |
| Responsive/theme/language                  | PASS in the completed root suite; 320/390/768/1280/1600px × English/Bangla × light/dark, image loading, proportional containment, no-JavaScript and reduced-motion checks.                                                                                                   |
| Image delivery                             | PASS; 400/640/900px WebP variants are 17,058 / 36,666 / 65,504 bytes. The 2,637,070-byte PNG master is not emitted directly.                                                                                                                                                 |
| Visual review                              | PASS for the reviewed source and screenshots: desktop English light/dark and Bangla mobile light/dark in the browser; existing completed-test screenshots for 320px English, 768px Bangla and 1600px English. All three teammates and devices remain visible.                |
| Console                                    | PASS in the completed automated root matrix; the inspected preview error log was empty before the later navigation block.                                                                                                                                                    |
| Further browser verification               | BLOCKED by the in-app browser URL policy when attempting the next language-link navigation. No alternate browser or navigation workaround was attempted. The full project-base E2E/GA4 suite was not rerun for this revision; earlier 192-test results below are historical. |
| Final source/format review                 | PASS; `git diff --check` and formatting checks.                                                                                                                                                                                                                              |

The browser policy block occurred after the root suite and successful desktop/mobile inspection. Remaining visual review used only screenshots already produced by the completed tests; the project-base build and file-based output checks required no further browser navigation. The local project-base preview server was restored for the user, without claiming a new interactive verification. No automated claim establishes cultural authenticity: the cloth is a contemporary interpretation, not a certified traditional outfit. No community endorsement is implied.

The existing immutable Yarn installation was reused. Unchanged external learning links, live GA4 delivery, physical devices, Safari/Firefox and Lighthouse scores were not checked in this image-only revision. The new textile references were consulted through web retrieval. No deployment or Git push was performed. Earlier validation entries retain their original scope and counts.

## Welcoming workspace: hoodie, orna, viewer-facing smiles and desk props

Updated the original hero using built-in ImageGen. The woman wears a teal hoodie and an ivory orna with a matching inner layer covering all hair. Both students smile casually toward the visitor, keeping separate laptops, keyboard hands and personal space. Coffee, a water bottle and a small indoor plant accompany the technical-diagram notebook. The first output left a visible hair patch and props beyond the desk edge; a second targeted edit corrected both. Only the inspected final output is shipped. English/Bangla alt text, the matching regression assertion, README, DESIGN, AI-Prompt, SKILL and illustration provenance reflect this latest direction. Earlier artwork records below are historical.

| Check                                 | Result                                                                                                                                                                                            |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type checking       | PASS; `yarn check`, 119 type-checked files, zero errors, warnings or hints.                                                                                                                       |
| Unit/content integrity                | PASS; 25 tests across 9 files (13 unit, 12 content).                                                                                                                                              |
| Root production regression suite      | PASS; 105 tests, 21 intentional duplicate skips: homepage, responsive/console and all 70 accessibility checks.                                                                                    |
| Full `/CSE-Compass/` production suite | PASS; 192 tests, 62 intentional skips; 122 E2E and 70 accessibility checks.                                                                                                                       |
| Root and project production builds    | PASS; 159 HTML pages, 179 files each; no build warnings.                                                                                                                                          |
| Build-output/SEO verification         | PASS at both bases; localized metadata, canonical URLs, English/Bangla/x-default hreflang, Open Graph, structured data, sitemap, robots, internal URLs/assets/anchors and unfinished-copy checks. |
| Responsive/theme/language             | PASS; 320, 390, 768, 1280 and 1600px × English/Bangla × light/dark; image loading, proportional containment, bilingual alt, no-JavaScript and reduced-motion coverage.                            |
| Image delivery                        | PASS; 400/640/900px WebP variants are 15,922 / 33,176 / 59,286 bytes. The 2,475,523-byte source PNG is not served directly; content-hashed image URLs changed.                                    |
| GA4 regression                        | PASS with the existing local stub; no requests to Google or live-property delivery claim.                                                                                                         |
| Browser/source review                 | PASS; source artwork and actual page screenshots inspected; preview error log empty; `git diff --check` passed.                                                                                   |

Manual screenshot review covered English desktop light/dark, Bangla mobile light/dark, Bangla tablet, narrow 320px and wide 1600px. Both students, the head covering, the complete laptops, notebook and new props stay visible without cropping. These visual judgments are manual, not inferred from passing alt-text assertions. The 62 full-suite skips avoid duplicate viewport matrices and a desktop run of a mobile-only navigation test; required widths were exercised.

This asset-only revision reuses the existing immutable Yarn installation and adds no dependencies, carousel, browser JavaScript or external image host. Unchanged external learning-resource links were not rechecked. Prior install/link results remain historical. No deployment, live GA4 receipt, physical-device/Safari/Firefox certification or Lighthouse score is claimed. The project-base production preview is restored after testing. Exact built-in edit prompts and the selected source are recorded in `docs/illustrations.md`.

## Focused technical-work artwork correction

Revised the mixed-gender hero after the user identified romantic cues. Both students now look down at their own laptops with neutral concentrated expressions, active keyboard hands and personal space; the notebook contains a small system flow diagram. A first edit incorrectly placed screen graphics on exterior lids; a second edit corrected these to silver backs facing the camera. The final artwork was manually inspected for gaze, expressions, device orientation, hand placement and equal participation before replacing the existing source PNG. The incorrect intermediate is not shipped. Bilingual alt text, its regression assertion and the product/engineering documentation now reflect the focused-work direction. Layout, loading strategy, routes and dependencies are unchanged.

| Check                                       | Result                                                                                                                                                                                  |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, lint, type checking             | PASS; 119 files, zero errors, warnings or hints.                                                                                                                                        |
| Unit and content integrity                  | PASS; 25 tests in 9 files (13 unit, 12 content).                                                                                                                                        |
| Root production regression suite            | PASS; 105 tests, 21 intentional duplicate skips: homepage, responsive/console and all 70 accessibility checks.                                                                          |
| Full `/CSE-Compass/` production suite       | PASS; 192 tests, 62 intentional skips, including 70 accessibility and 122 E2E checks.                                                                                                   |
| Root/project builds and output verification | PASS; 159 HTML pages and 179 files each, no warnings, broken local URLs/assets, missing metadata or placeholder defects.                                                                |
| Image delivery                              | PASS; three 400/640/900px WebP variants: 14,992 / 31,916 / 56,472 bytes. The 2,477,826-byte PNG master is not emitted directly. Content-hashed URLs changed with the artwork.           |
| Responsive/language/theme                   | PASS; five-width English/Bangla/light/dark matrix, proportional containment, image loading, alt text and no-JavaScript/reduced-motion checks.                                           |
| SEO and analytics regression                | PASS; build verification covers localized metadata, canonical, hreflang, Open Graph, structured data, sitemap and robots. Existing bounded GA4 stub tests pass; no live delivery claim. |
| Final formatting and diff checks            | PASS.                                                                                                                                                                                   |

Actual screenshots were inspected at 320, 390, 768, 1280 and 1600px widths, including English desktop light/dark and Bangla mobile/tablet. Both students remain engaged with their own devices, and the entire composition stays visible. A tablet capture coincided with the root-to-project preview switch and missed its newly requested responsive image; the page was reloaded at the correct project URL and the complete tablet review passed. The refreshed preview console was empty. This was a local test-server transition, not an unresolved asset defect.

This small revision reused the existing immutable Yarn installation; no dependencies or external links changed. Earlier install/external-link results remain historical, not newly claimed checks. No deployment, live GA4 receipt, physical-device/cross-browser certification or Lighthouse score is claimed. The final project-base preview was restored after testing; image provenance and prompts are in `docs/illustrations.md`.

## Inclusive homepage study-partners illustration

Replaced the lone-male hero with an original scene of a woman and man learning computing together, each actively using their own laptop. The static shared scene gives both equal visibility without carousel slides, autoplay, controls or additional JavaScript. English/Bangla alt text describes the scene; proportional sizing and containment preserve both students on small screens. The future-self encouragement, homepage order, AI illustration and existing brand remain unchanged. README, DESIGN, AI-Prompt, SKILL and illustration provenance were updated.

| Check                                | Result                                                                                                                                                                                                                 |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `yarn install --immutable`           | PASS; Yarn 4.17.1, no dependency changes. Approved retry allowed access to the existing user cache.                                                                                                                    |
| Formatting, lint and type checking   | PASS; 119 files, zero errors, warnings or hints.                                                                                                                                                                       |
| Unit/content tests                   | PASS; all 25 tests across 9 files (13 unit, 12 content).                                                                                                                                                               |
| Full production browser suites       | PASS at both `/` and `/CSE-Compass/`: 192 passed and 62 intentional skips each; 70 axe accessibility checks and 122 E2E checks per suite.                                                                              |
| New illustration regression coverage | PASS; accurate bilingual alt text, eager/high-priority hero, responsive source widths, lazy supporting image, reserved dimensions, complete loading, proportional containment and no-JavaScript/reduced-motion access. |
| Responsive/theme/language matrix     | PASS; 320, 390, 768, 1280 and 1600px × English/Bangla × light/dark; no horizontal overflow, distorted image or out-of-container artwork.                                                                               |
| Root/project production builds       | PASS; 159 HTML pages, 179 files, no build warnings. Project-base output rebuilt and verified after the root suite.                                                                                                     |
| SEO/internal output verification     | PASS; localized metadata, canonicals, hreflang, Open Graph, JSON-LD, sitemap, robots, local URLs/assets and anchors.                                                                                                   |
| GA4 regression / console             | PASS with the existing local analytics stub; no live Google delivery claim. Automated console checks passed; inspected preview error log was empty.                                                                    |
| Asset delivery                       | PASS; 400/640/900px WebP files are 14,260 / 29,726 / 52,480 bytes. The 2,103,100-byte PNG master and retired mentor asset are not emitted into the production asset folder. No new dependency or client bundle.        |
| Source review                        | `git diff --check` passed; no unfinished copy in the changed homepage/provenance. The new PNG is visible to Git, not ignored.                                                                                          |

Actual viewport screenshots were reviewed for English desktop light/dark, Bangla mobile light/dark, English tablet, narrow 320px and wide 1600px layouts. The full students/laptops composition, caption, theme separation and wrapping were inspected; mobile review included taller viewport captures to see the artwork and caption together. Theme/language controls were exercised in the browser. Keyboard navigation and no-JavaScript/reduced-motion behavior were covered by the automated suite, not claimed as a separate assistive-technology certification.

The initial artwork and one transparency retry had a baked-in checkerboard. Neither is shipped. A final targeted ImageGen edit supplied an intentional blue-gray matte backdrop, presented with a theme-aware border. Source artwork, generation mode and prompts are recorded in `docs/illustrations.md`; no transparency is falsely claimed for this asset. The earlier mentor remains only as a historical source/style reference.

The 62 skips are the existing duplicate viewport matrices, tablet duplicate and desktop skip of the mobile-only navigation test; all required widths were actually exercised. External learning links were unchanged and were not rechecked for this illustration-only revision; the earlier link-review limitations below still apply. No deployment, physical-device/Safari/Firefox test, live GA4 verification or Lighthouse score is claimed. GitHub Pages remains the hosting target.

## Refined identity, final-year project standards and three career paths

This revision replaces the unclear homepage introduction, refines the native SVG mark/favicon and original social card, and adds Network Engineering, Hardware Engineering and Software QA & Testing. Fourteen careers now have complete career, comparison, roadmap, experiment, resource and AI guidance. The new seven-stage final-year project guide includes four project-type adaptations, a worked traceability example, readiness evidence and real English/Bangla Markdown downloads. It is linked from roadmaps, foundations, goal guides and the footer without adding another homepage section. At the user's request, the temporary third-language work was removed: only English (default) and Bangla ship.

| Check                                                  | Result                                                                                                                                                                                                                                                             |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Immutable Yarn installation                            | PASS; Yarn 4.17.1. The user-cache sandbox restriction required an approved retry; no dependency changes or install warnings.                                                                                                                                       |
| Formatting, lint, type checking                        | PASS; 119 checked files, zero type errors, warnings or hints.                                                                                                                                                                                                      |
| Unit tests                                             | PASS; 13 tests.                                                                                                                                                                                                                                                    |
| Content-integrity tests                                | PASS; 12 tests, including bilingual new-career/project content, exact family membership, tailored roadmaps, references and two-language scope.                                                                                                                     |
| Full project-Pages production browser suite            | PASS; 188 tests, 62 intentional skips.                                                                                                                                                                                                                             |
| Full root production browser suite                     | PASS; 188 tests, 62 intentional skips.                                                                                                                                                                                                                             |
| Accessibility subset of each full suite                | PASS; 70 axe WCAG A/AA checks, including new EN/BN guidance/career routes and light/dark themes under desktop/mobile projects.                                                                                                                                     |
| E2E subset of each full suite                          | PASS; 118 tests covering navigation, comparison, goals, new career route graphs, project templates/downloads, language switching, keyboard/no-JavaScript behavior and layout matrices.                                                                             |
| Root/project production builds and output verification | PASS; 159 HTML pages and 179 files; no build warnings, broken local routes/assets, missing metadata or unfinished HTML copy found.                                                                                                                                 |
| GA4 integration                                        | PASS using a local stub with Google requests blocked. Existing view/navigation/source events plus language switching, project-guide entry and evidence-template events are verified. No live delivery claim.                                                       |
| Responsive/theme/language matrix                       | PASS; 320, 390, 768, 1280 and 1600px, English/Bangla, light/dark. Homepage, goals and new project/career views are covered, including overflow and breadcrumb alignment.                                                                                           |
| External URLs                                          | 47 HTTP 200 responses; 3 automated HTTP 403 restrictions; no confirmed broken links. Linux Journey was additionally inspected through web retrieval and resolves to its official LabEx successor. Both ACM policy URLs remained access-restricted on that recheck. |
| Browser console                                        | No errors in the inspected successful local preview; automated console/page-error checks also passed.                                                                                                                                                              |
| Git/source review                                      | `git diff --check` passed. All new source files, including the download endpoint, appear in the untracked-file list and are not ignored. No third-language source/assets or TODO/FIXME/placeholder copy remain in source/public files.                             |

The 62 skips avoid repeating three explicit 20-case viewport matrices under the mobile project, plus one tablet duplicate and one desktop skip of a mobile-only navigation test. They do not omit the required mobile widths. Tests were run against actual production previews, not just the development server.

### Review and fixes

The source-integrity test originally assumed every guidance reference belonged to a goal. It was extended to include final-year project ownership while still rejecting unused sources. A Git ignore exception now protects the authored download endpoint. The language expansion was fully removed following the user's updated scope. A Bangla spelling error in the hardware guide was corrected during final editorial review.

After the spelling correction, formatting/lint/type checking and all 25 unit/content tests passed again. Both root and `/CSE-Compass/` sites were rebuilt and output-verified; all eight hardware-career E2E/accessibility regression checks passed on each updated production output. The project-base production preview was restored after testing.

Actual viewport screenshots were inspected for the refined logo/homepage; desktop project heading and aligned breadcrumbs; dark project stage with section navigation and evidence panel; Bangla mobile heading, controls and project-type disclosure with visible focus; and Bangla tablet hardware-career introduction. The social card was inspected for exact brand/tagline text. The test teardown briefly stopped the local server during manual review; the preview was restarted and the affected visual review was completed successfully. This was a local test-lifecycle interruption, not a production route defect.

### SEO, architecture and deployment

Generated-output verification checks unique localized titles, descriptions and canonicals; English/Bangla/x-default hreflang; Open Graph assets; WebSite/Article/BreadcrumbList JSON-LD validity; sitemap inclusion; base-aware robots sitemap URLs; internal URLs, images/srcset and same-page anchors. Both root and project-base output passed. New guides and downloadable templates are statically generated from typed bilingual data; no backend, React hydration, progress storage or new package dependency was introduced. The SVG identity is resolution independent; original responsive homepage WebP assets remain unchanged. The new social card is about 938 KiB and is metadata-only, not a homepage-body download.

GitHub Pages workflows and Yarn-only quality gates were inspected and retained. No push, deployment, DNS change or hosting migration was performed. Browser coverage is Chromium desktop/mobile emulation plus actual in-app-browser inspection, not physical-device, Safari/Firefox or screen-reader certification. No Lighthouse score, live GA4-property receipt or automated accessibility-conformance guarantee is claimed. The genuine remaining verification limitation is the two access-restricted ACM reference pages; their links were not silently counted as verified successes.

Earlier revision records below remain historical and retain their original counts/results.

## Goal guidance, Mobile App Development and UX/UI Engineering

This revision adds seven complete bilingual goal guides and two complete career paths. The site now generates 133 HTML pages: eleven careers across eight families, with matching career/roadmap/experiment/AI pages, plus the goal hub and seven goal-detail pages in English and Bangla. README.md, DESIGN.md, SKILL.md and AI-Prompt.md describe the implemented architecture and content rules.

| Check                                            | Result                                                                                                                                                                                                         |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `yarn install --immutable`                       | PASS; Yarn 4.17.1, no dependency changes or warnings. The first sandboxed attempt could not access the existing user cache; the approved cache-access retry succeeded.                                         |
| `yarn format`, `yarn format:check`, `yarn lint`  | PASS                                                                                                                                                                                                           |
| `yarn typecheck`                                 | PASS; 109 checked files, zero errors, warnings or hints                                                                                                                                                        |
| Unit tests                                       | PASS; 13 tests                                                                                                                                                                                                 |
| Content-integrity tests                          | PASS; 9 tests, including complete bilingual goal fields, source relationships and the two new career graphs                                                                                                    |
| Root production-preview browser suite            | PASS; 124 tests, 42 intentional skips                                                                                                                                                                          |
| `/CSE-Compass/` production-preview browser suite | PASS; 124 tests, 42 intentional skips                                                                                                                                                                          |
| Accessibility subset                             | PASS; 46 axe WCAG A/AA checks across desktop/mobile, critical EN/BN routes and both themes                                                                                                                     |
| Responsive matrices                              | PASS; 320, 390, 768, 1280 and 1600px × English/Bangla × light/dark for homepage and representative goal/new-career pages; no horizontal overflow                                                               |
| Root and project-Pages builds                    | PASS; 133 pages, build output inspected for warnings, none found                                                                                                                                               |
| Root and project-Pages `yarn verify:build`       | PASS; 151 files, valid local routes/assets/anchors and metadata checks                                                                                                                                         |
| Browser journeys                                 | PASS; all seven guides in both languages, both new careers across four route types, homepage/roadmap/lost → goal → career roadmap, equivalent-language switching and no-JavaScript keyboard section navigation |
| GA4 abstraction                                  | PASS with a local stub; bounded goal navigation and official-source clicks, plus existing destination view and homepage events; Google requests blocked                                                        |
| External learning and guidance links             | 38 HTTP 200 responses; 4 HTTP 403 restrictions explicitly reported as REVIEW, with no confirmed broken URLs                                                                                                    |
| `git diff --check` and unfinished-copy scan      | PASS; no source/public TODO, FIXME, Lorem ipsum or coming-soon copy                                                                                                                                            |

The 42 skips are not defects: two explicit 20-case viewport matrices and one tablet test run once under Chromium rather than duplicating under the mobile project; the mobile-only navigation test skips desktop. Full suites were rerun after fixes rather than treating a successful build as completion.

### Visual, language and UX review

Actual in-app-browser captures were inspected for the desktop goal hub and grouped choices; a full guide stage with its sticky index in light/dark; the mobile starting-point chooser; Bangla tablet heading, breadcrumbs and reality note; Bangla mobile weekly action; the new UX/UI mobile career introduction; and the homepage's compact goal entry. Browser console inspection returned no errors. Automated tests additionally cover every goal's route and essential sections and exercise the existing illustrations, orientation map and useful loop.

The new pages reuse semantic theme tokens and do not add client framework hydration. Essential guide content and native section navigation work with JavaScript disabled. Mobile platform references explicitly say they are alternatives, not sequential learning levels. Goal sources have scope and review dates; their reference status does not promise free application, testing or publication costs. Bangla prose was reviewed alongside the English source; technical terms remain in English where natural. No external linguistic certification is claimed.

### SEO, performance and GitHub Pages

Generated-output checks covered unique localized titles, descriptions and canonicals; EN/BN/x-default alternates; Open Graph images; parseable WebSite/Article/BreadcrumbList data; sitemap membership; robots sitemap URL; local assets, `srcset` and same-page anchors. The goal tests verify equivalent-language URLs and Article markup. Both root and `/CSE-Compass/` builds and actual production previews were tested.

The added goal-detail stylesheet is approximately 4.9 KB uncompressed. Static guide data is rendered at build time; no new JavaScript bundle or third-party UI package is introduced. Existing responsive WebP assets remain approximately 11–78 KB. This is an asset inspection, not a claimed Lighthouse or real-user performance score.

GitHub Pages remains the deployment target; its workflows were inspected and retained. No push, production deployment, DNS change or live GA4-property verification was performed. No hosting service migration was made.

### Defects caught and fixed

- The initial localized-field test incorrectly rejected the valid short branch label “PhD”; it now checks nonempty labels while retaining stronger prose and stage-content checks.
- New career entries were placed after existing entries to preserve existing comparison defaults.
- Mobile platform resources initially inherited misleading Learn/Practice/Go Deeper labels and a three-resource slice. The shared component now supports explicit platform alternatives, exposes all four choices, and provides them across the mobile roadmap. Browser assertions protect the labels.
- Visual review caught adjacent homepage label/sentence/link text without whitespace. Explicit spaces and English/Bangla regression assertions now protect both the goal entry and restart encouragement.

### Genuine limitations

The latest complete external check verified 65 links with HTTP 200. Linux Journey alone returned HTTP 403 and remains a manual-review item; the checker does not call it a verified success. Remote pages and requirements may change after review.

Tests use Chromium desktop/mobile emulation, not physical Android/iOS devices or Safari/Firefox. Axe and keyboard/visual checks are not full assistive-technology certification. Live analytics delivery requires the owner's production GA4 configuration and was not claimed.

## Earlier visual revision

This records the checks actually completed for the theme, homepage and shared-layout revision. It is a point-in-time report, not a claim that future changes or every browser are covered.

| Check                                                      | Result                                                                                                            |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `yarn install --immutable`                                 | PASS; Yarn 4.17.1, no dependency warnings                                                                         |
| `yarn format` / `yarn format:check`                        | PASS                                                                                                              |
| `yarn lint`                                                | PASS                                                                                                              |
| `yarn typecheck`                                           | PASS; 0 errors, warnings or hints                                                                                 |
| Unit and content tests (`yarn check` includes `yarn test`) | PASS; 19 tests: 13 unit and 6 content-integrity                                                                   |
| Root development-site E2E and accessibility                | PASS; 64 tests, 22 intentional skips                                                                              |
| `/CSE-Compass/` production-preview E2E and accessibility   | PASS; 64 tests, 22 intentional skips                                                                              |
| Accessibility subset                                       | PASS; all 18 axe WCAG A/AA checks on desktop/mobile, light/dark and critical EN/BN routes                         |
| Responsive matrix                                          | PASS; 320, 390, 768, 1280 and 1600px × English/Bangla × light/dark; no horizontal overflow or console/page errors |
| Root and project-Pages production builds                   | PASS; 101 HTML pages, no build warnings                                                                           |
| Root and project-Pages `yarn verify:build`                 | PASS; 118 files; local URLs, image `srcset`, same-page anchors, SEO and placeholder checks                        |
| Analytics dispatch                                         | PASS with a local GA4 stub; no Google requests; career/roadmap views emitted once at destination                  |
| External learning resources                                | 20 verified HTTP 200; Linux Journey returns HTTP 403 and remains a manual-review limitation                       |
| `git diff --check` and unfinished-content scan             | PASS; no source/public TODOs or placeholder copy                                                                  |

The 22 browser skips are deliberate: the explicit 20-case viewport matrix and one tablet case run once under Chromium rather than repeating under the mobile project, and the mobile-only menu test is excluded from desktop. None are skipped defects.

## Visual and interaction review

The supplied `themes-sample.png` was inspected against the running site. Viewport-sized browser captures were reviewed for the desktop hero in both themes, English small-mobile hero, Bangla mobile/tablet layouts, orientation map, AI illustration, useful loop and production career breadcrumb alignment. The generated characters remain legible in both themes. The page now uses eight focused content sections and retains the complete career, roadmap and experiment directories.

Automated interaction checks additionally exercise menu keyboard Enter/Tab/Escape, mobile-to-desktop resizing, no-JavaScript navigation, mentor disclosure, equivalent-language routes, stored theme selection, starting-point/recovery guidance, the lost flow, comparisons, career → roadmap → experiment and image loading. The browser console was also inspected directly on production output without errors.

## SEO and delivery

The generated-site verifier checks unique titles/canonicals, descriptions, English/Bangla/x-default hreflang, Open Graph images, parseable structured-data JSON, sitemap membership, robots sitemap URL, local route/asset existence and unfinished copy. Both `/` and `/CSE-Compass/` builds passed. Images are generated as six responsive WebP variants, approximately 11–78 KB each; the site adds no image-library hydration or remote font dependency.

The existing GitHub Pages workflows remain the deployment target. No live deployment, DNS change or push was performed. Live GA4 reporting was not tested because a real measurement ID is not configured. Automated accessibility checks plus visual/keyboard review are not a screen-reader certification or a claim of testing Safari/Firefox or physical devices.

## Defects found and resolved during the loop

- Orientation captions fell into a narrow grid-number column; titles/captions now share a wrapper.
- Inherited prose-list margins staggered breadcrumbs and grid items; these components explicitly reset margins.
- A closed mobile disclosure also hid desktop navigation; breakpoint-aware state and no-JavaScript fallback restore it.
- One small amber label had 4.42:1 contrast on the new surface; its color was darkened and all accessibility checks rerun successfully.
- View events relied on particular inbound links; destination-based career/roadmap events now cover direct navigation without duplicate view events.
- External-link restrictions were previously labeled as passes; they are now explicitly reported as `REVIEW`.

Two new test-selector mistakes and one verifier invocation before a build finished were corrected by fixing the selectors/sequencing and rerunning the affected checks. The passing results above are the final reruns, not the initial attempts.
